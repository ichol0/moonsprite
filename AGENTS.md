# MoonSprite 开发规则

中文 | [English](AGENTS.en.md)

本文件是所有人工开发者和 AI 代理必须遵守的项目级规则。每个连续需求首次开始修改前阅读 [文档索引](docs/README.md)；只有 D2/D3、发布、性能或架构任务才加载 [AI 与开发自动工作流](docs/agent-workflow.md) 的对应章节，再按任务风险阅读相关行为契约。连续 Debug 不重复读取已经确认的规则，也不得遍历 `docs/archive/` 或历史更新日志归档。

## 文档语言边界

- `AGENTS.md`、`docs/README.md` 和不带 `.en.md` 后缀的中文文档是 AI 的唯一日常规则源。`AGENTS.en.md`、`README.en.md`、`CHANGELOG.en.md` 及其他 `*.en.md` 仅供人工英文阅读，不得因页面顶部的 English 链接自动打开。
- 普通需求、UI 修改、Debug、审查和测试，无论用户使用中文还是英文交流，都只读取中文规则与契约；可以按用户使用的语言回复。搜索文档时必须排除 `*.en.md`，不得同时读取、搜索或比较同一内容的中英文版本。
- 开发期间只更新中文规则、契约和维护文档。只有用户明确要求翻译、英文文档维护、双语一致性审计，或已经进入 `dev.X` 发布并执行本周期英文同步时，才允许读取对应英文镜像；只处理本周期实际变化的配对文件，禁止全仓双语扫描。

## 默认工作状态

- 项目默认始终处于当前 `dev.X` 的连续开发阶段，直到用户明确要求“发布 dev.X”“准备发布”或“生成开发版”。工作流只有开发中和发布两个阶段，不设置中间稳定批次。
- 当前开发周期起点提交 SHA 是审计基线；发布审计该基线之后的累计变化，日常只验证本次实际修改的文件。
- 除用户明确调用 `$moonsprite-code-architect` 外，所有普通需求、UI 修改、Debug、审查、测试和发布工作都由当前主 Agent 直接完成，禁止创建、分派或保留任何子 Agent。
- `$moonsprite-code-architect` 只在用户明确要求整项目架构审计、重大里程碑架构复查或大型跨模块重构规划时调用，默认只读；每次只允许创建一个架构师实例，且该实例禁止继续创建或分派后代 Agent。普通需求、UI 修改和连续 Debug 不得自动调用，也不得恢复按需求自动分派子 Agent 的工作流。

## 修改流程

1. 先确定改动属于纯视觉、普通交互、共享状态、坐标与选区、撤销、文件格式、Rust 或平台集成，再按风险选择检查。
2. 修改限制在相关模块，禁止借局部问题重构无关代码；发现架构债时记录到发布审计，不在 Debug 中自行扩大范围。
3. Bug 只有在曾经复发、难以人工发现、涉及共享核心算法，或可能破坏坐标、撤销、文件数据与平台安全时，才必须补自动化回归测试和回归矩阵；普通视觉、间距、文案和简单显隐问题由用户直接验收。
4. 开发期间不要求逐条更新 `CHANGELOG.md`。发布 `dev.X` 时根据本周期基线后的完整 diff 集中整理，仍需为每个可独立说明的有效变化保留独立条目，并遵守 [完整更新日志规则](docs/release/changelog-policy.md)。
5. 视图移动、缩放、旋转、栏目布局等界面状态不得进入文档撤销历史。
6. 坐标转换必须复用统一几何函数，不得在组件内重复计算旋转、缩放和偏移。
7. 不执行强制推送，不提交生成目录、安装包、用户工程、恢复文件或密钥。
8. 只有用户明确要求发布或生成安装包时才构建安装包。

## dev.X 开发中

- 每个需求优先完成可体验实现并尽快交给用户。D0、D1 和普通连续 Debug 不以自动测试或 `check:dev` 作为默认收尾动作；D2 只在实现稳定、准备结束本次需求时进行一轮最低检查；D3 在交付可能影响文档数据或核心语义的实现前进行一次最小安全检查。
- 开发检查按 D0-D3 分层：文档、文案和纯 CSS 为 D0，不运行应用检查；普通组件、菜单、弹窗、低频交互和简单显隐 Debug 为 D1，默认由用户直接验收，只有新增跨模块依赖或明确需要类型确认时才运行定向边界检查或 `--strict`；一般 Core、Store、Shared 和快捷键逻辑为 D2，稳定后最多运行一次相关 TypeScript 项目检查，不默认运行行为测试；坐标、选区、撤销、文件格式、持久化、平台安全、共享核心算法，以及曾复发或难以人工发现的 Bug 为 D3，使用 `pnpm check:dev -- --risk=high <源文件...> <直接相关测试文件...>`。Rust 与缩略图代码运行对应 `cargo check`。
- 开发期定向测试只用于 D3、明确复发问题、共享核心算法或用户明确要求测试的任务。默认只列出一个最直接的测试文件；只有改动同时跨越互相独立的高风险契约时才允许增加，不得为了“更放心”追加相邻的大型测试文件、完整测试或广泛回归。
- 同一需求的连续 Debug 复用已完成的风险判断、文档上下文和检查结果。D0-D2 中间轮次不重复检查，等用户确认现象解决或实现准备结束时再做唯一一次必要检查；除非出现确定的编译或测试错误、改动范围扩大或风险等级升级，否则不得重跑同一门禁。
- 用户负责布局、手感、动画、视觉和需求符合度的主观验收。除非用户明确要求，不运行浏览器自动点击、截图对比、桌面自动验收或完整构建。
- 开发期间不运行 `check:maintenance`、完整测试、完整 CI、性能基准，不更新发布日志、性能历史或一般回归矩阵，也不为每个问题提交、推送或打包。
- 只有任务触及组件写文档、撤销模型、工程编解码、恢复、Core 依赖或根 Store 职责时，才在本轮最低检查中追加一次 `pnpm check:architecture`；普通 UI、文案和连续 Debug 不运行该全仓静态门禁。
- 日常 D0/D1 只读本文件、中文文档索引和直接相关的中文契约，不加载完整工作流；发布、性能、历史审计、ADR 和英文镜像只在任务确实触及时读取。首次进入需求时检查一次 `git status`；连续 Debug 只检查本次目标文件的现有差异，避免反复扫描整棵工作树。
- 有确定的编译或测试错误时可以修复后重跑；没有确定证据时不得继续猜测式修改。连续两轮仍未解决，必须报告现象、根因判断和下一步，不得自行扩大调查或重构。

## dev.X 发布

- 只有用户明确要求发布时才进入本阶段。发布前先运行 `pnpm check:release-performance`，每次只处理一个候选、最多两轮；低风险候选可自动尝试，高风险候选必须等待确认，无收益就恢复并记录未采纳。
- 随后先集中更新中文日志、行为契约、架构状态和必要回归矩阵，再仅对本周期实际变化且已有英文镜像的文件做一次定向同步，最后运行 `pnpm check:release`；不得为同步英文而扫描未变化的文档。桌面门禁和安装包只在明确需要时执行。发布完成后记录 SHA 作为下一个周期基线。
- 详细发布步骤见 `docs/agent-workflow.md` 和 `docs/release/release-checklist.md`，普通开发不加载这些细节。

## 性能审计

- 普通需求和连续 Debug 不运行性能基准；性能只在版本发布、专项、明确退化或用户明确要求时执行。
- 性能审计每次只处理一个稳定候选、最多两轮，必须超过噪声门槛并通过正确性测试；高风险像素、坐标、撤销和格式优化先等待确认。
- 详细套件、基线、环境指纹和 CI 只读 `docs/agent-workflow.md` 与 `docs/testing/performance-baseline.md`；`check:release` 不偷偷运行基准。

## 可复用 UI

- 创建渲染器 UI 前，先检查 `src/renderer/src/components/ComponentLibrary.tsx` 和 `src/renderer/src/styles.css` 的组件库区域。
- 优先复用 `NumberInput`、`ThemedSelect`、`ColorPicker`、现有按钮、弹窗、栏目和分段控件。
- 新增可复用组件时，必须加入 `COMPONENT_LIBRARY_ENTRIES` 并在 `previewRenderers` 提供可交互预览。
- 预览应覆盖默认、选中、禁用和交互状态。
- 生产界面的操作、状态、工具和导航图标只能使用组件库已登记的像素图标组件或像素资产，严禁新增或继续使用 `lucide-react` 等矢量图标库、任意内联矢量图标、图标字体或 Emoji 图标。组件库缺少所需图标时，必须先新增像素图标、登记条目并补齐预览，再在业务界面使用；不得用矢量图标临时代替。修改已有界面时，触及范围内的既有矢量图标也必须替换为组件库像素图标。
- UI 容器保持直角，主要选中和操作颜色统一使用 `#2979FF`。
- 1024 x 640 及主要桌面尺寸的最终视觉检查由用户完成；只有用户明确要求时才自动截图验收。

## 模块边界

- `core/` 保存可独立测试的像素、选区、格式和几何算法，不依赖 React。
- `store/` 管理文档会话与操作，不直接实现视图绘制。
- `components/` 管理展示和输入，不复制核心算法。
- `platform/` 是渲染器访问 Tauri 的唯一边界。
- `src-tauri/` 负责系统文件、窗口、恢复、剪贴板、缩略图和打包集成。
- 不为超大文件增加新的无关职责；新逻辑优先提取到明确模块，并由风险相称的测试保护。
- 组件不得直接修改 `SpriteDocument`、`DocumentSession`、像素、dirty、revision、invalidation 或缓存版本，也不得调用原始 `mutateActive`、`pushHistory` 或直接控制 `HistoryStack`；所有文档写入和历史提交必须经过 Store 领域命令或 `begin/update/commit/cancel` 文档事务。
- 实时预览事务必须同时处理确认、取消、切换文档和组件卸载；取消路径恢复基线，确认路径只产生一次 dirty、历史和失效提交。
- `encodeProject`/`decodeProject` 只属于文件与恢复边界，不得用于撤销快照。历史保存受影响领域的最小前后状态，不依赖工程格式。
- 一次工程打开只允许一次完整解码；初始合成、缩略图和缓存必须复用已解码文档。异步保存、恢复和工程编码不得在派发 Worker 前同步准备整份文档。
- 保存与恢复错误必须进入可观测错误通道，不得使用空 `catch` 或仅注释的 `catch` 静默吞掉。
- `core/` 生产运行时依赖图禁止循环；渲染键只能使用领域 revision 与轻量标量，不得序列化像素、cel 表面或整份文档。
- `WorkspaceState` 只由 `store/workspace-state.ts` 中的会话、切片、工具、颜色、视图选区、历史、动画、Tilemap、图层、剪贴板、工程 IO、恢复和 UI 契约组合；不得在 `workspace.ts` 重建巨型根接口或绕过领域契约增加命令。
- `check:dev` 对 Renderer 代码只做本次变更文件的定向依赖检查；`pnpm check:boundaries` 不带文件参数时才扫描完整 Renderer，发布和架构审计使用完整扫描，不使用按文件白名单。`pnpm check:architecture` 检查完整架构契约；现有历史债只允许登记在 `scripts/architecture-debt-budget.json` 的数字预算中，实际数量与预算必须相等，预算只能递减、不能延期，到期必须清零。

## 命令入口

```powershell
# D2 实现稳定后：只检查本次文件，不默认运行行为测试
pnpm check:dev -- <本任务文件...>

# D1 只有明确需要类型确认时才使用 --strict
pnpm check:dev -- --strict <本次实际修改文件...>

# D3 高风险开发：类型检查 + 最小直接回归测试
pnpm check:dev -- --risk=high <源文件...> <相关测试文件...>

# 仅在触及受保护架构边界时追加
pnpm check:architecture

# 只检查本次变更文件的依赖方向（check:dev 会自动调用）
pnpm check:boundaries -- --files <本次修改的 Renderer 文件...>

# dev.X 发布：完整正确性与维护门禁，不含性能基准
pnpm check:release

# 版本发布、里程碑或性能专项
pnpm check:release-performance
```

`check:fast` 和 `check:integration` 仅作为旧调用兼容别名，分别等价于 `check:dev` 和 `check:release`，新任务不再使用旧名称。
