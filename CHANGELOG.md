# 变更记录

本文件记录 MoonSprite 的用户可见变化。格式参考 Keep a Changelog；开发阶段使用 `DEV`，正式发布时再归入具体 SemVer。

## DEV.1

### 交互改进
- 调色板颜色格恢复为两套明确状态：前景色右上角、背景色左下角角标永久保留；左键或右键点击的颜色格显示临时蓝色描边，颜色格失焦后仅隐藏描边。
- 修复热更新或焦点切换漏掉修饰键释放后，所有工具指针错误保持为吸管的问题；画布指针事件现在会重新校正 `Alt/Ctrl` 状态。
- 修复图层栏目漏掉 `Alt` 释放事件后持续显示复制指针的问题；图层栏现在会用最新鼠标事件校正复制状态。
- 颜色值编辑器按紧凑的通道编辑结构重新排版：模式、HEX 和当前色位于顶部，每个通道使用一条彩色滑轨及右侧数值输入；首选项中的两种透明背景颜色并排显示。
- 新建图层默认快捷键设为 `Shift+N`；图层和图层组默认无显示颜色，属性底部可选择无颜色、预设色或自定义色，图层行以最左侧竖条标识；描述使用统一多行输入和自定义悬浮提示，并随 `.moonsprite` 工程保存。
- 光标首选项明确为优先使用本地指针；当前操作缺少对应本地指针时继续回退到 MoonSprite 软件指针。
- 修复 Windows 资源管理器拖入工程或图片无响应；恢复 Tauri 官方窗口拖放入口，并支持拖入 `.moonsprite`、`.ase`、`.aseprite`、PNG、JPEG、WebP、BMP 与 GIF。
- 预览栏目滚轮缩放改为以鼠标位置为锚点；选区缩放和旋转拖拽期间统一显示移动指针。
- 顶部任一菜单打开后，悬停其他顶部菜单会直接切换展开；新增默认 `X` 交换前景色与背景色，并支持自定义快捷键。
- 选区变换修饰键统一为 `Shift` 固定比例、`Ctrl` 整数硬边缘倍数，两者可组合；旋转选区时按住 `Shift` 会按 45° 吸附到八个方向。
- 修复滚轮缩放预览尚未提交时立即撤销可能恢复旧视图的问题；Undo/Redo 现在先同步视图预览，视图状态不进入文档历史。
- 修复 `.moonsprite` 从资源管理器拖入后仍停留首页而看似没有打开的问题，并统一规范化 Windows 与 `file://` 拖入路径。
- 新增曲线 `Ctrl+M`、色相/饱和度 `Ctrl+U`；其余可执行菜单命令均登记到快捷键设置，未指定的命令默认留空供用户自定义。
- 收紧选区四角的斜向缩放与旋转交互：旋转只占用角点附近 28 CSS px，紧邻缩放区且无空白；超过该范围后恢复套索输入，不再阻挡已有选区周边像素的继续选择。
- 修复高倍缩放下角点命中半径被强制扩大到一个文档像素的问题；外侧缩放范围现在贴合可见白色手柄，越过手柄外缘 1px 即进入对应旋转区域。
- 修复套索最终选区固定缺少最右列和最下行的问题；路径边界像素现在完整纳入选区。
- 修复选区角点缩放与旋转之间被边缘移动带插入的问题；离开斜向缩放区后会立即进入对应角的旋转过渡带，边缘移动只在角部范围之外生效。
- 恢复弹窗和表单的输入框键盘导航；`Tab` 切换到下一个输入框，`Shift+Tab` 反向切换，快捷键录入不受影响。
- 扩大选区四角斜向缩放命中区，沿角点向外移动时直接切换到对应旋转指针，不再经过空白区域。
- 修复悬浮预览栏目八向调整尺寸的边角命中衔接，并在改变窗口尺寸后立即按新内容区重新适配作品。
- 另存为与导出进度改为在 Windows 文件选择器确认路径后出现，标题分别显示“正在另存为”和“正在导出”；取消文件选择不再闪出进度框。
- 顶部“视图”菜单更名为“窗口”，集中提供颜色、调色板、图层和预览栏目的显示切换及工具栏左右位置；栏目标题右键可隐藏栏目或移动到左、右、底部和悬浮区，布局随当前工作区实时保存。
- 修复选框整条边被缩放区占用而导致移动选框与四角旋转不可用；八个缩放点、边缘移动带和四角外侧旋转区现在互不抢占。
- 画布左下角的尺寸与镜像状态使用同一文字颜色，并根据其下方画布或工作区背景自动切换深色与浅色。
- 新建工程改为只读取剪贴板图片头部尺寸，避免大图 RGBA 跨进程传输造成数秒延迟；修复曲线弹窗删除按钮图标被拉长错位。
- 修复镜像视图铺满工作区时边缘内容被裁切、扩大画布预览中画布外图层未受遮罩，以及选区变换命中范围向右下偏移。
- 重新切分更新后的指针源工程，并统一校准默认、绘制、移动、复制、吸管、缩放和旋转视图指针的 32×32 热点。
- 新建工程复用后台缓存的系统资源信息并提前加载画布模块，点击创建后不再重复等待系统内存探测。
- 调色板色块选中后可通过 `Delete` 或 `Backspace` 删除，且不会同时误删图层或选区。
- 选区八个缩放点使用更大的统一命中范围，四角外侧缩放与旋转区域连续衔接，不再出现无响应空洞。
- 修复视图镜像无视觉变化、恢复工程后黑屏、翻转后残留画笔直线预览的问题。
- 首选项新增默认开启的“画笔 Shift 拉直线”；选区切换绘制工具后隐藏变换点，并扩大四角外侧旋转交互范围。
- 快捷键设置支持清空绑定，修复单独录入 Ctrl 变成 `Ctrl+Control`，并统一滚动条样式。
- 数值输入框保留单位紧贴数字，同时整块输入区域都可作为鼠标拖选起点；已有路径的普通保存不再显示进度框。
- 快捷键设置统一覆盖文件、工具、选区、图像、图层、视图和编辑命令；顶部菜单的快捷键提示与实际配置同步。
- 新增视图镜像及状态提示；镜像仅改变显示，不反向绘画、选区和其他文档操作，且与缩放、平移和旋转一样保持在文档撤销历史之外。
- 选区四角旋转控制点改为更大的无形外侧命中区，保留旋转交互并减少画面遮挡。
- 移动工具可同时移动多个已选图层，实时预览位移，并统一作为一次撤销操作。
- 保存工程时显示紧凑、可关闭的进度提示，仅在实际保存阶段更新；图层属性支持 Enter 保存并关闭，Esc 关闭当前弹窗。
- 快捷键设置新增笔刷尺寸和修饰键条目；工具栏提示与模式提示读取当前快捷键配置。
- 首选项提供缩放工具的默认平滑或百分比缩放模式；缩放工具拖动时按住 `Shift` 会临时按百分比缩放，不再切换为画布抓手。
- 选区变换命中区重新分层：角点优先缩放、边中段独占，旋转区不再与缩放区重叠。
- 修复旋转与水平/垂直镜像组合后的指针锚定缩放异常，视图菜单新增“旋转视图”和“镜像视图”子菜单，并将“复位视图”统一恢复缩放、平移、旋转和镜像。
- 曲线编辑器新增当前图层基线直方图叠加，修复曲线 SVG 右键后的白色焦点边框，并整理通道按钮布局。
- 首选项新增“应用”按钮，可保存设置而不关闭窗口；背景颜色与调色盘颜色值入口统一为可切换 RGB、HSV、HSL、Gray、透明度、HEX 和滑块的颜色编辑组件。
- 使用更新后的指针源工程重新切分资源；位置 5,2 的移动指针已同步更新，并保留本地资源缺失时的软件内置回退。

### 已有能力

- 建立 Windows 像素画编辑器的完整开发版工作流。
- 支持图层、选区、笔刷、调色盘、工作区、恢复和多格式文件处理。
- 支持 `.moonsprite` 文件关联、Windows 缩略图和 NSIS 安装包。

### 工程维护

- 建立中文开发文档、行为契约、回归矩阵和发布检查表。
- 维护门禁新增架构计划状态一致性校验，防止已完成任务继续保留过期的阶段性文案。
- 性能验证改为 P0-P4 风险分级，支持按画布尺寸和场景定向执行；普通功能修改不再机械运行或登记完整性能基准。
- 固定前端依赖版本，避免 `latest` 引入不可预测升级。
- 建立本地 Git 基线和 GitHub 持续集成流程。
- 增加隔离运行的 Windows 桌面回归测试，覆盖启动、工具切换、旋转视图、高级模式、菜单与无效撤销。
## DEV

### 维护性改进

- 提取快捷键和编辑器首选项的类型化持久化边界，集中处理默认值、非法值和范围限制。
- 修复撤销栈撤销/重做后的内存计数错误，并保证历史回调失败时不会丢失历史条目。
- 为 `.moonsprite` 工程清单增加统一的版本迁移入口，为后续格式升级保留兼容边界。
- 为上述边界增加快捷键、首选项、历史和工程清单回归测试。
- 平台文件写入改为先落盘临时文件再原子替换，降低保存、恢复和工作区写入中断时的数据损坏风险。
- 关闭窗口改为单一待处理请求，连续点击关闭不再重复触发确认和退出超时。
- 集中应用目录下的图库、色板、笔刷和工作区路径，减少平台存储位置分散导致的维护风险。
- 集中渲染器本地存储与工作区布局校验，避免应用外壳直接读写和解释分散的持久化数据。
- 拆分文档文件服务与恢复队列，统一保存格式规则，并避免自动保存与放弃恢复并发时重新生成已删除草稿。
- 集中管理选区与图层剪贴板快照；系统剪贴板不可读取或数据无效时，粘贴会可靠回退到软件内部最近一次复制内容。
- 集中画布临时输入状态和选区缩放、轴约束、缩放级别等手势规则，降低工具交互互相影响的维护风险。
- 集中浮动栏目拖拽、缩放、停靠预览和布局持久化的共用行为，并统一栏目布局配置的校验入口。
- 将画布、首页和组件库改为按需加载，降低首屏主入口体积，保持核心文档状态由 `App` 和 store 管理。
- 将调色板尺寸、颜色比较、标记对比色和多选排序规则集中到独立模块，降低栏目 UI 与调色板行为的耦合。
- 将图层栏的树形展平、嵌套组关系和拖放目标判定集中到独立核心模块，并为未来时间轴保留稳定的图层身份边界。
- 将图层移动、分组、解组和跨组排序统一为可撤销的核心命令，降低拖放与撤销历史互相影响的风险。
- 将画布光标、透明棋盘和选区预览的纯视觉规则从画布组件中抽离，并增加核心回归测试。
- 将 `.moonsprite` 工程格式升级为 v2：旧工程自动迁移为单帧时间轴，并为后续 Aseprite 式帧和 cel 数据保留独立格式边界。
- 将 Tauri 的剪贴板、二进制文件与资源查询命令拆分为独立平台模块，并补充 Rust 输入校验测试。
- 强化 Windows WebView2 回归脚本：隔离应用数据目录并验证 Tauri IPC、画布旋转、工具切换与高级模式，避免测试污染用户恢复数据。
- 新增 128、512、1024 画布的自动交互性能基准；统一画布渲染几何、缓存舞台尺寸并减少分块绘制的重复变换，连续绘制开始不再触发冗余全局刷新。
- 性能基准新增按区域 React 提交测量；颜色、色板、图层和预览栏目改为按实际数据变化刷新，1024 画布平移、旋转后缩放和连续绘制的 React 提交耗时中位数分别下降约 24%、33% 和 18%。
- 将文档标签、工具栏、工具属性栏、状态栏、画布主机和编辑器布局外壳从 `App.tsx` 拆分为独立刷新边界；1024 画布平移、旋转后缩放和连续绘制的 React 提交耗时相对上一阶段进一步下降约 82%、93% 和 66%。
## 未发布

- 所有统一数值输入框支持 `+ - * /` 和括号表达式，提交后自动计算并遵守原字段范围。
- “窗口 > 旋转视图”改为顺时针/逆时针旋转 90°，两项命令可在快捷键设置中自定义。
- “文件”菜单新增最近打开文件子栏；首页最近记录支持 MoonSprite、Aseprite 与常见图片，并始终显示文件名和后缀。
- 首选项新增光标、背景和编辑页面，可配置本地像素指针与比例、四种笔刷预览、透明棋盘格、滚轮缩放和铅笔直线实时预览。
- 自动恢复设置整合为时间预设下拉框，运行时使用所选间隔，关闭后不再执行定时或失焦恢复写入。

- 修复快捷键冲突提示只在刚修改后存在的问题；冲突项现在会在重新打开设置时持续显示无法触发图标。
- 将画笔 Shift 连线选项更名为“直线连接模式”，迁移到快捷键设置的修饰键页面。
- 新增水平镜像视图和垂直镜像视图，镜像只影响显示，画布输入仍跟随视觉位置，并显示当前镜像状态。
- 未保存工程执行普通保存时直接调用系统保存流程，不再先弹出 MoonSprite 保存进度框。
- 图层属性弹窗支持按 Enter 保存并关闭。
- 调整画布尺寸预览会显示新边界内原本位于画布外的图层内容。
- 曲线调整支持 RGB、R、G、B 独立通道，并统一使用三次贝塞尔曲线。
- 选区缩放手柄允许跨越对侧边界，跨越后自动进入对应的镜像变换。
- 修正跨边缩放的镜像轴：改为使用实际拖动的边或角点，而不是目标矩形中心；曲线通道支持删除选中的中间控制点。
