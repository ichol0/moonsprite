# 回归测试矩阵

本文件记录高风险用户工作流。修复 Bug 时先补充场景和自动化测试，再修改实现。状态使用“已有”“待补”“手动”。

| 领域 | 场景 | 预期 | 状态 |
| --- | --- | --- | --- |
| 历史 | 无历史时按 `Ctrl+Z` | 无副作用，dirty 不变化 | 核心测试与桌面 E2E |
| 历史 | Alt 复制图层后撤销 | 一次撤销删除复制层及移动 | 已有 Store 测试 |
| 视图 | 平移、缩放、旋转后撤销 | 视图不进入文档历史 | 旋转已有本机桌面 E2E，平移与缩放待补 |
| 视图 | 镜像视图、缩放工具拖动策略 | 镜像只改变显示；输入、缩放与平移仍使用未镜像文档方向 | `view-geometry.test.ts`、`canvas-input.test.ts` |
| 视图 | 切换镜像视图 | 合成画面立即水平镜像，文档像素和操作历史不改变 | 待补桌面场景 |
| 视图 | 分别开启、关闭水平和垂直镜像 | 提示文字与实际状态一致，垂直镜像快捷键和菜单均可用 | `view-geometry.test.ts`，待补桌面场景 |
| 视图 | 旋转或镜像后在指针位置缩放 | 缩放前后同一文档像素保持在指针下，旋转与两种镜像组合不回弹 | `view-geometry.test.ts` |
| 选区 | 角点、边中段和四角外侧命中 | 角点优先缩放，边中段独占，旋转区不与缩放区重叠 | `canvas-input.test.ts` |
| 视图 | 镜像后移动画笔、选区和形状预览 | 预览位置与视觉镜像后的落点一致，不残留在原位置 | 待补桌面场景 |
| 坐标 | 旋转视图后绘制和选区 | 像素、选区、预览位置一致 | 已有几何测试，待补 UI |
| 坐标 | 旧工程带图层偏移 | 复制、粘贴、变换不偏移 | 待补 |
| 剪贴板 | 系统剪贴板不可读或数据无效 | 回退到本软件最近一次选区复制内容，且复制来源不被粘贴修改 | `clipboard-service.test.ts` |
| 平台剪贴板 | 图像尺寸为零、溢出或超过限制 | 拒绝异常输入，不调用系统剪贴板 | Rust `platform_clipboard` 测试 |
| 指针 | 切换工具、按键松开或热更新后继续绘制/指向图层 | 指针立即更新且不叠加；画布和图层栏目都以最新指针事件校正丢失的 Alt 状态，不会误显示吸管或复制指针 | `canvas-visuals.test.ts`、`canvas-input.test.ts`，待补桌面场景 |
| 指针 | 重新切图后检查 32×32 热点 | 默认、绘制、移动、复制、吸管、缩放和旋转指针的实际操作点与热点契约一致 | 手动；热点坐标见 `pointer-modifiers.md` |
| 指针 | 隐藏或锁定图层绘制 | 禁止操作且无预览像素 | 待补 UI |
| 指针 | 指针命中选区缩放或旋转区域 | 工具像素预览隐藏，只显示当前变换指针 | 待补 UI |
| 选区 | 新建、加选、减选、交集 | 原蚂蚁线在操作中保持 | 已有算法测试，待补 UI |
| 选区 | 套索闭合后检查右侧和下侧边界 | 套索路径经过的像素属于最终选区，矩形路径的左、上、右、下四边均完整，不得固定缺少最右列或最下行 | `tools.test.ts` |
| 选区 | 魔棒按住预览并松开 | 预览区域与最终选区一致 | 已有算法测试，待补 UI |
| 选区 | 内容超出画布后移动回来 | 边界外像素完整保留 | 已有 Store 测试 |
| 选区 | 框选后切换到画笔 | 隐藏变换控制点和旋转命中，只保留蚂蚁线并限制在选区内绘制 | 待补桌面场景 |
| 变换 | 四角外侧旋转命中 | 在不显示蓝点的前提下提供角点附近 28 CSS px、互不重叠的紧凑旋转命中区 | `canvas-input.test.ts`，待补桌面场景 |
| 变换 | 八个缩放点与四角旋转区域命中 | 缩放点具有舒适且一致的命中范围；离开缩放区后连续进入最近角点的旋转区，不出现无响应空洞 | `canvas-input.test.ts`，待补桌面场景 |
| 变换 | 四角斜向缩放向外移动 | 斜向缩放命中区大于边中点命中区；越过命中边界后直接切换对应旋转指针，中间不出现移动选框或默认指针 | `canvas-input.test.ts`，待补桌面场景 |
| 变换 | 从四角斜向缩放点持续移入外侧 | 离开 5 CSS px 外侧缩放区后立即进入对应旋转指针；到角点 28 CSS px 后恢复普通套索输入，中间不得出现无响应空洞 | `canvas-input.test.ts` |
| 变换 | 从可见角点手柄移入外侧象限 | 可见 `8×8` 手柄及紧邻容差内仍为斜向缩放；越过手柄外缘后的第一个可用像素立即变为对应旋转指针，不得被 18px 内侧易点击范围继续占用 | `canvas-input.test.ts`，浏览器真实指针采样 |
| 浮动栏目 | 预览栏目八向调整尺寸 | 四条边与四个角的命中带贴合浮窗边框、互相连续，拖动方向与指针一致 | 手动，待补组件测试 |
| 浮动栏目 | 预览与其他栏目调整尺寸 | 所有悬浮栏目共用同一套边框内外对称命中带；预览不随视口比例单独缩放或把命中区压向内部 | 手动，待补组件测试 |
| 文件 | 另存为与导出进度提示 | Windows 文件选择器确认前不显示应用进度；确认路径后分别显示“正在另存为”“正在导出”，完成后自动关闭 | `workspace.test.ts` |
| 画布输入 | 拖拽完成、取消和修饰键切换 | 同一手势只完成一次；轴约束、缩放级别和选区整数缩放保持稳定 | `canvas-input.test.ts`，待补桌面 E2E |
| 画布输入 | 画笔已有直线锚点后执行翻转快捷键 | 直线预览立即清除，下一笔不连接翻转前的锚点 | 待补 Store/UI |
| 停靠布局 | 旧布局、重复栏目、栏目移动和最小尺寸 | 自动补齐栏目、保持唯一顺序并限制尺寸，移动前后不丢失布局 | `panel-layout.test.ts` |
| 笔刷 | 非基础笔刷调整尺寸 | 不闪烁基础笔刷预览 | 待补 UI |
| 笔刷 | 自定义笔刷三种模式 | 预览与实际平铺一致 | 已有工具测试 |
| 颜色 | 调色盘右键选择背景色 | 色相、滑条、选择点同步 | 已有颜色测试，待补 UI |
| 调色板 | 多选色块拖拽排序和尺寸预设 | 色块作为连续块移动，插入目标唯一，尺寸预设稳定 | `palette-layout.test.ts` |
| 调色板 | 选中色块后按 `Delete` 或 `Backspace` | 删除全部选中色块并阻止同时删除图层或选区；操作可一次撤销 | `palette-layout.test.ts`、Store 测试，待补 UI |
| 调色板 | 左右键选择颜色格并失焦 | 左键设置前景色、右键设置背景色；被点击颜色格显示临时蓝色描边，失焦后只隐藏描边，前景/背景角标持续保留 | `palette-layout.test.ts`，待补 UI |
| 图层 | 文件夹嵌套和跨层拖放 | 插入线层级与结果一致 | `layer-panel-layout.test.ts`、`layer-operations.test.ts`，待补专用桌面场景 |
| 图层 | 多选图层后使用移动工具拖动 | 所有可编辑已选图层实时一起移动，并作为单一撤销事务 | 待补桌面场景 |
| 图层 | 修改显示颜色与描述后保存、重开工程 | 图层行色标和悬停描述保持；旧工程缺少字段时正常打开；修改作为一次属性历史可撤销 | `format.test.ts`，待补组件场景 |
| 快捷键 | 自定义椭圆选区、图像、视图、图层和修饰键 | 设置、菜单右侧提示、模式提示与实际执行使用同一命令定义 | `shortcuts.test.ts` |
| 快捷键 | 录入单独的 Ctrl、Alt 或 Shift | 保存为单个修饰键，不重复为 `Ctrl+Control`；允许清空绑定 | `shortcuts.test.ts` |
| 快捷键 | 快捷键长列表滚动 | 使用组件库统一滚动条，内容不超出设置窗口 | 待补 UI |
| 快捷键 | 重开设置后检查重复绑定 | 冲突状态由完整配置重新计算；先匹配命令正常使用，后续无法触发项显示警告图标和说明 | `shortcuts.test.ts`，待补 UI |
| 快捷键 | 直线连接模式开关 | 开关位于“快捷键设置 > 修饰键”，名称为“直线连接模式”，修改后画笔与橡皮擦的 `Shift` 连线立即同步 | `file-preferences.test.ts`，待补 UI |
| 快捷键 | 重新打开快捷键设置检查直线连接模式 | 显示当前快捷键，不显示独立开关；可清空或重新绑定 | `shortcuts.test.ts`，待补 UI |
| 弹窗 | Esc 关闭与图层属性 Enter 提交 | Esc 关闭当前弹窗；图层属性 Enter 保存并关闭 | 待补桌面场景 |
| 弹窗 | 同一表单存在多个文本或数值输入框 | `Tab` 按界面顺序切换到下一个输入框，`Shift+Tab` 反向切换；快捷键录入框仍负责录入快捷键 | `form-focus.test.ts` |
| 保存 | 保存过程反馈 | 保存时显示紧凑且可关闭的反馈；仅有实际阶段时更新进度，成功后自动关闭 | 待补桌面场景 |
| 保存 | 已有文件路径的普通保存 | 直接完成保存，不弹进度框；首次保存或另存为才显示紧凑反馈 | 待补 Store/UI |
| 保存 | 尚未落盘工程执行普通保存 | 直接调用系统保存流程，不先显示 MoonSprite 保存进度框 | 待补 Store/UI |
| 视图 | 水平或垂直镜像后绘制、吸色、缩放和平移 | 显示位置与鼠标操作位置一致；文档像素不被镜像修改；左下角持续显示当前镜像轴 | `view-geometry.test.ts`，待补 UI |
| 视图 | 镜像视图放大至画布铺满视口 | 离屏场景覆盖镜像后全部可见区域，画布四边不被裁切 | `canvas-render-plan.test.ts` |
| 图像 | 调整画布尺寸时预览画布外图层 | 新画布边界包含的原画布外像素在裁剪预览中可见，提交结果与预览一致 | 待补核心/UI |
| 图像 | 扩大画布时预览原画布外图层 | 图层像素可见，但旧画布外、新画布内区域统一叠加黑色遮罩 | `canvas-resize-preview.test.ts` |
| 调整 | RGB 与单通道曲线 | RGB 主曲线后依次应用 R/G/B 贝塞尔曲线；单通道不改变其他通道 | `adjustments.test.ts` |
| 调整 | 曲线直方图 | 按当前活动图层基线统计 RGB、R、G、B 分布，透明像素不计入，直方图不修改文档 | `adjustments.test.ts` |
| 调整 | 曲线通道切换与控制点删除 | 通道按钮有明确色彩倾向；中间点可稳定删除，端点不可删除 | 待补组件场景 |
| 变换 | 缩放手柄跨过对侧边界 | 继续缩放并切换对应水平/垂直镜像；预览与最终提交一致且一次撤销完成 | `canvas-input.test.ts`、`tools.test.ts` |
| 变换 | 边或角点跨过对侧边界 | 对称轴取被拖动的边；斜向拖动以被拖动角点为轴交点 | `canvas-input.test.ts`、`selection.test.ts` |
| 动画格式 | v1 工程打开、v2 保存、无效帧和 cel | 旧工程变为单帧时间轴，未知版本拒绝，cel 关系不重复 | `animation.test.ts`、`project-format.test.ts` |
| 工作区 | 调整、切换、复位、重启 | 当前与初始布局语义正确 | 待补 Store/UI |
| 工作区 | 显示、隐藏、停靠、浮动和调整栏目 | 四个栏目独立保存可见性与布局；旧工作区自动补齐，隐藏栏目不占停靠空间且可从“窗口”菜单恢复 | `workspace-layout-preferences.test.ts`，待补桌面场景 |
| 文件 | 拖入 `.moonsprite`、`.ase`、`.aseprite` 或图片 | 统一规范化 Windows 与 `file://` 路径，直接打开并进入编辑器 | `document-drop.test.ts`，待补 Tauri E2E |
| 文件 | 另存为不同格式 | 扩展名、系统类型和编码一致 | 已有格式测试，待补 E2E |
| 启动 | 文件关联启动工程 | 不停留首页，不重复弹窗 | `test:tauri`、`test:desktop` |
| 关闭 | 连续点击关闭并取消 | 同一时间只出现一次确认，取消后旧超时不得退出软件 | 已有 Rust 状态测试，待补桌面 E2E |
| 恢复 | 自动保存进行中放弃恢复 | 等待写入结束后删除，已放弃草稿不得被晚到写入重新创建 | 已有恢复服务测试 |
| 恢复 | 从首页打开恢复工程 | 正常进入编辑器并可操作，不出现黑屏或无活动会话 | 待补 Store/UI |
| 新建 | 点击“创建画布”进入编辑器 | 复用已缓存的资源信息并预加载画布模块，不因重复系统内存探测停留在首页数秒 | `resource-info-cache.test.ts`，待补桌面计时 |
| 新建 | 打开新建画布弹窗且剪贴板含图片 | 只读取图片头部尺寸，不跨进程传输整张 RGBA；宽高立即更新 | `NewDocumentDialog.test.tsx`、Rust `platform_clipboard` 测试 |
| 输入 | 带单位的长数值输入框从右向左拖选 | 输入框空白区域仍属于文字编辑区，可跨单位左侧选择任意数字片段 | 待补组件测试 |
| 选区 | 八个变换手柄在边界两侧命中 | 使用连续文档坐标，左右与上下命中距离对称，不向右下偏移 | `view-geometry.test.ts`、`canvas-input.test.ts` |
| 选区 | 选框边缘移动与四角外侧旋转 | 边缘内外侧优先移动选框；离开边缘带后连续进入最近四角旋转区，光标与按下操作一致 | `canvas-input.test.ts`，待补桌面场景 |
| 选区 | 套索工具在已有选区四角附近继续框选 | 斜向缩放与旋转只占用角点附近的紧凑区域；越过旋转区后立即恢复套索输入，周边像素可继续选择 | `canvas-input.test.ts` |
| 变换 | 旋转选区时按住 `Shift` | 旋转角度吸附到 0°、45°、90° 等八个方向；松开 `Shift` 恢复连续角度 | `canvas-input.test.ts` |
| 变换 | 斜向缩放时分别按住 `Shift`、`Ctrl` 或两者 | `Shift` 固定宽高比，`Ctrl` 使用整数硬边缘倍数，两者组合时按原比例进行整数倍缩放 | `canvas-input.test.ts` |
| 撤销 | 滚轮缩放预览尚未提交时立即执行 Undo/Redo | 先同步提交当前视图预览，再撤销或重做文档操作；缩放、平移和旋转均保持不变 | `workspace.test.ts`、`view-preview-lifecycle.test.ts` |
| 文件 | 将 `.moonsprite` 从资源管理器拖入软件 | 普通 Windows 路径和 `file://` 路径统一规范化、去重并打开工程，不被重复拖入事件吞掉 | `document-drop.test.ts`，待补桌面 E2E |
| 文件 | 从资源管理器拖入 `.moonsprite`、`.ase`、`.aseprite` 或受支持图片 | Tauri 官方窗口拖放事件作为主入口，Drop 路径只提交一次并直接进入编辑器 | `document-drop-events.test.ts`、`document-drop.test.ts`，待补桌面 E2E |
| 预览 | 在预览窗口内将鼠标停在作品局部并滚轮缩放 | 缩放前后的同一作品像素保持在鼠标下，不再固定以预览中心放大 | `preview-geometry.test.ts` |
| 指针 | 拖动选区的缩放或旋转变换 | 按下拖拽期间统一显示移动指针，松开后恢复命中区域对应指针 | `canvas-visuals.test.ts`，待补桌面场景 |
| 菜单 | 点击打开任一顶部菜单后横向移动鼠标 | 无需再次点击即可切换并展开鼠标当前所在的其他顶部菜单 | `menu-behavior.test.ts` |
| 菜单 | 打开“文件 > 最近打开文件” | 子栏列出全部保留的最近工程与图片，名称始终包含真实后缀，点击后使用统一文件打开路径 | `home-history.test.ts`，待补 UI |
| 视图 | 从“窗口 > 旋转视图”执行 90° 旋转 | 顺时针与逆时针分别改变视图 90°，不切换工具、不写入文档历史，且可绑定或清空快捷键 | `shortcuts.test.ts`，待补 UI |
| 输入 | 在任意统一数值输入框输入 `2+3*4`、括号或负数 | 回车或失焦后安全计算表达式，再执行字段范围限制；非法表达式与除零恢复原值 | `numeric-expression.test.ts`、`NumberInput.test.tsx` |
| 首选项 | 修改自动恢复时间、光标、笔刷预览、透明背景和编辑设置 | 旧设置兼容读取；确定后实时应用，重启后保持；关闭自动恢复时不执行定时或失焦写入 | `file-preferences.test.ts`，待补 UI |
| 首选项 | 点击“应用”保存设置 | 设置写入本地但首选项窗口保持打开；再次点击“确定”才关闭 | `PreferencesDialog.test.tsx` |
| 颜色 | 打开颜色值按钮并切换 RGB、HSV、HSL、Gray | 默认 RGB；每种模式有透明度、HEX 输入和滑块，修改结果同步到对应颜色 | `color-values.test.ts`、`ColorValueControl.test.tsx` |
| 背景 | 修改透明棋盘格大小与两种颜色 | 主画布、画布尺寸预览、作品预览和笔刷预览使用同一配置，重置恢复中等默认值 | `canvas-visuals.test.ts`，待补 UI |
| 颜色 | 按下前后景色交换快捷键 | 默认 `X` 一次交换前景色与背景色，并可在快捷键设置中修改或清空 | `shortcuts.test.ts`、`workspace.test.ts` |
| 快捷键 | 打开曲线与色相/饱和度并自定义其他菜单操作 | `Ctrl+M` 打开曲线，`Ctrl+U` 打开色相/饱和度；其余已注册命令默认留空且可在快捷键设置中绑定 | `shortcuts.test.ts`，待补 UI |
| 快捷键 | 执行新建图层 | 默认 `Shift+N` 新建图层，菜单提示和快捷键设置显示同一绑定，用户可修改或清空 | `shortcuts.test.ts` |
| 颜色 | 打开颜色值编辑器调整任意通道 | 预览条和滑块轨道显示对应颜色变化，数值输入位于滑块右侧且修改结果一致 | `ColorValueControl.test.tsx`，待补视觉检查 |
| 图层 | 新建图层并设置显示颜色与描述 | 新图层默认无色标；设置预设色或自定义色后仅在图层行最左侧显示竖条；描述使用统一多行输入并通过自定义提示展示 | `document.test.ts`、组件测试，待补桌面视觉检查 |
| 缩放 | 缩放工具按住 `Shift` 后拖动 | 不进入抓手平移；本次拖动临时按百分比级数缩放，松开后恢复首选项中的默认缩放模式 | `canvas-input.test.ts` |
| 调整 | 曲线弹窗通道、删除和重置按钮 | 按钮在窄视口内保持同高、居中且不挤压曲线编辑区 | `AdjustmentDialog.test.tsx`，1024 x 640 视觉验证 |
| 性能 | 128、512、1024 画布平移、缩放、旋转后缩放和连续绘制 | 固定输入下输出帧分位数、>25ms 比例、主绘制与指针处理耗时；视图操作不进入撤销历史 | `scripts/canvas-performance.mjs`、`canvas-render-plan.test.ts`、`canvas-composite-cache.test.ts` |
| 性能工具 | 统一数值输入框支持表达式后运行完整画布基准 | 按“画布宽度/画布高度”的稳定可访问名称填写尺寸，不依赖输入框内部的 `type` 实现 | `pnpm bench:canvas -- --full` |
| 安装 | 全新用户安装与首次启动 | 无黑屏、无重复弹窗 | 发布前手动；桌面脚本使用隔离应用数据验证启动路径 |

## 新增规则

- 每个用户报告的回归问题至少增加一行。
- “待补”场景被修改时必须转为自动化测试，不能只改为“手动通过”。
- 像素结果使用数组或图像像素断言；交互结果同时断言状态与可见反馈。
- 性能回归写入 `performance-baseline.md`，不混用功能正确性断言。
