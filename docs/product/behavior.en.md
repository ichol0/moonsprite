# Product Behavior Contract

[中文](behavior.md) | English

This document defines stable user-visible behavior for the current MoonSprite development build. Detailed interactions are supplemented by `docs/interactions/`. Implementations must not change these rules without updating the contract and tests.

## Projects and Files

- Users can open multiple projects simultaneously and switch or duplicate views from the top project bar.
- The top Window menu centrally manages view aids, panel visibility, and tool-rail position. Panel layout changes save immediately to the active workspace.
- Help > Roadmap shows confirmed long-term directions and completion status in Chinese and English. Dithering brushes, theme editing, slices, and animation loop sections are marked complete. Linked layers remain planned. Pencil smoothing is no longer listed. Completed items sort first.
- Help > Changelog and Home > News show only content from the most recent formally packaged release and must not include Unreleased content from the root `CHANGELOG.md`. Home News shows that release's version, publication date, and short summary; activating it opens the same complete update shown under Help > Changelog. Both surfaces use the matching `docs/changelog/<version>.md` archive as their source.
- `.moonsprite` stores complete project state and opens through file selection, Windows file association, or Explorer drag and drop. Drop handling goes through one platform service, with HTML, Tauri Webview, Window, and Rust window events as mutual fallbacks; if one channel fails, retry when the window regains focus. Every channel shares format filtering and path deduplication. Successful drop must enter the matching editor instead of remaining on Home.
- MoonSprite creates `gallery` and `exports` beside the application by default. First Save and Save As for a new project open `gallery`; image, GIF, timelapse video, and palette-image export open `exports`. Save As provides file name, format, and location. The location button selects the directory used as the native save dialog's default path. Export Settings uses Export Region to distinguish Canvas, Frames, and Slices; choosing Slices allows all slices or one specific slice. Preferences can override default save and export locations independently. Clearing an override follows the executable-root defaults again. Normal Save for an existing file always writes to its original path.
- `.ase` and `.aseprite` open through file selection, drag and drop, and file association. Save or export follows the format explicitly chosen by the user.
- PNG, JPEG, WebP, BMP, and GIF open through file selection or drag and drop as single-layer pixel documents; GIF currently reads the first frame. Imported images retain their source path. While editing remains within the original single layer and single frame, normal Save overwrites the original path in the original format. After adding project structures that the image cannot preserve, such as layers, groups, frames, masks, text, layer styles, slices, project brushes, or timelapse records, normal Save must switch to a `.moonsprite` project. Save As still follows the user's explicit format and path.
- Starting from a file association enters the target project directly rather than stopping on Home.
- Opened or saved `.moonsprite`, `.ase`, `.aseprite`, PNG, JPEG, WebP, BMP, and GIF files enter Recents. Home and File > Recent Files always show the real file name and extension. If a recent path no longer exists, notify the user and remove the record automatically. If the path exists but cannot be parsed, keep both file and recent record and show Remove from Recents for explicit handling. Delete Project in Gallery is a separate destructive action and requires confirmation before deleting the disk file.
- Home displays the complete file list first, then loads thumbnails progressively with at most three concurrent tasks. One failure must not block other projects. `.moonsprite`, `.ase`, `.aseprite`, PNG, JPEG, WebP, BMP, and GIF all show thumbnails. Ordinary images generate a smoothed preview with maximum side 512 px. If a project lacks an embedded preview, generate the same bounded thumbnail serially in the background, cache successful results on disk by file fingerprint, and reuse them in the current session. Do not decode multiple large files at once and slow Home. Recovery records also generate covers progressively but must not rewrite full previews into autosave files. Home covers use smooth scaling consistently. Right-clicking an ordinary project card opens its containing folder.
- `Ctrl+wheel` on Home switches and persists Small, Medium, and Large project layouts. Small is a compact single-line list containing only title and path. Medium retains the standard thumbnail list. Large uses four enlarged cover cards per row and shows title, file information, path, and time. Home section management can choose any folder as a peer custom section and reads only supported projects and images from that folder's first level. Recent, Gallery, Recovery, and all custom sections can be reordered together by drag and persist. Removing a custom section from Home must not delete disk content. Four pixel buttons in the upper-right open the Moon Pixel Community QQ group, Steam, GitHub, and Language. The first three open through the system browser. Language opens a formal selection dialog and persists Simplified Chinese or English on confirmation.
- When New Project opens and the system clipboard contains an image, read the image-header width and height immediately into the size fields. Do not decode or transfer the complete pixel data across processes for this step. After creation, focus enters the new project's canvas rather than palette or another panel control, and the Tileset panel is hidden by default.
- The executable-root `brushes` directory stores global image brushes. The brush library appears as a right panel by default and accepts the Add button, file drag and drop, or `Ctrl+B` selection-to-PNG creation. Imported and selection brushes retain RGBA and intrinsic dimensions with a `256px` width and height limit. Manual ordering is persisted separately and does not affect project dirty state or undo history. System textures are available only to Paint Bucket; Pencil, Eraser, and Line use basic shapes or image brushes only.
- Projects from abnormal shutdown enter Recovery. After explicit discard, they must not be repeatedly prompted or restored automatically. Recovery data is retained for seven days by default and configurable from `1-365` days under Preferences > Files. At application startup, expired recovery metadata and project-data files are deleted as pairs even after a normal previous shutdown.
- Pixel-grid and custom-grid toggles and custom-grid parameters are current-project display settings. Saving and reopening restores them without reusing another project's grid state. Zoom, pan, rotation, and mirroring remain current-session state only.
- New Canvas provides Record Drawing Process, whose default comes from Preferences and initially is off. File > Timelapse Video controls recording, quality, export frame rate, and playback multiplier per project. While enabled, every committed drawing or content edit records one equally weighted video frame, independent of real time, and excludes zoom, pan, and panel operations. Records save in `.moonsprite`, can be cleared, and export to MP4 or WebM.
- File > Project Information shows canvas, color mode, layer/group/frame counts, stroke count, edit-operation count, active drawing time, created and modified times, and timelapse-record frame count.
- The executable-root `scripts` directory stores global Lua scripts. The Scripts submenu at the bottom of File lists ordinary UTF-8 `.lua` files from the directory's first level by name, refreshes when File opens, and ends with Open Scripts Folder. The menu sends only a script file name to the platform. The platform rejects traversal, subdirectories, symlinks, and non-Lua files and does not run arbitrary paths through a system file picker.
- The script compatibility layer provides `Point`, `Rectangle`, `Color` with RGB and HSV fields, `Image`, current selection, active `Sprite/Layer/Cel`, `app.pixelColor`, `app.transaction`, `app.refresh`, `app.useTool` for line and eraser, `print`, `app.alert`, and generic `Dialog` controls. `Dialog` supports common controls such as color and radio buttons. `show()` blocks until close; `show { wait = false }` opens a non-blocking script dialog whose events continue in the original Lua VM. `Sprite(width, height)` creates a new document, and active Sprite `newLayer()` / `newCel()` create ordinary pixel layers. Structural changes return typed new-layer or new-document results to the Store and never write back a whole project.
- Initial script pixel target is restricted to the active ordinary pixel layer at the active frame. Text, Tilemap, free-tile, layer mask, locked targets, and layers above `4,194,304` local pixels are rejected explicitly. Lua exposes no file, network, process, package-loading, or debug library and enforces memory, per-image, session cumulative image allocation, modification, instruction, runtime, source-size, and output-size limits. Every successful `app.transaction` forms its own undo step. Writes outside transactions merge by script file name. Local drawing commits pixel deltas. `app.activeCel.image` and `position` changes commit atomically as one Cel-surface history entry. Script-created layers in the active document also enter through undoable Store history. An uncaught error applies no result from the call. A failed transaction caught by `pcall` rolls back only that transaction and retains earlier successful transactions.
- Before each persistent script-dialog callback, confirm the same original document, layer, and frame, then rebuild the Lua baseline from current Cel surface, revision, selection, foreground, and background. Undo, redo, or another committed edit on the same target therefore does not invalidate the dialog. The callback result must still match the rebuilt baseline exactly. Switching document, layer, or frame rejects application and destroys the session. Scripts with `print` or `app.alert` output and failed scripts display a result dialog; successful scripts with no output report only in the bottom status bar.
- The executable-root `extensions` directory stores installed `.msext` packages. An extension is a ZIP whose root contains `manifest.json`. The manifest uses `schemaVersion: 1`, a valid unique `id`, name, version, optional description, author, `apiVersion`, compatibility `.lua` `entry`, named `commands[]`, declarative `panels[]`, existing-menu `menuItems[]`, and new top-level `topMenus[]`. Before installation, limits apply to package size, file count, command count, panel count, menu-contribution count, and total extracted bytes. Reject traversal, absolute paths, duplicate paths, symlinks, missing command entries, duplicate contribution IDs, unknown built-in menus, invalid menu positions, references to unknown commands, and invalid manifests.
- Preferences > Extensions can add an extension, open its folder, enable or disable, and uninstall. Enabled state lives in a state file under the extension directory and does not affect project dirty or undo history. Double-click from Explorer, startup arguments, and dragging into MoonSprite all use the same atomic staging installation flow. Failed replacement retains the old installation. MoonSprite has no fixed top-level Extensions menu. Enabled extensions may insert commands at the start or end of File, Edit, Select, Image, Layer, Window, or Help, and may add top-level menus before or after built-ins. Extension panels toggle under Window > Panels and are rendered as floating panels by MoonSprite.
- Extensions cannot inject arbitrary React, DOM, CSS, JavaScript, or native code. Every menu and panel may reference only manifest-validated commands. The platform resolves `extension:<id>:<commandId>` and reuses the restricted Lua runtime. Compatibility `entry` continues as `extension:<id>` under File > Scripts. Legacy commands not referenced by menus or panels also fall back to the script list. The Renderer neither receives nor executes extension paths. Disabling, uninstalling, or failing a security check removes matching menus and panels immediately. Panel visibility and position are local application state and do not enter project dirty or document history.
- Beyond the compatibility layer, the script runtime provides the MoonSprite-specific `mse.*` namespace. `mse.apiVersion`, `mse.status`, `mse.capabilities`, and `mse.isSupported()` support capability detection. The first version implements only read-only `mse.document.info()` and `mse.selection.info()`. Tilemap, free tile, animation loop, palette, pattern brush, layer style, slice, workspace, file IO, and UI endpoint shapes are registered in advance. Calling an unimplemented endpoint returns an explicit error and must never pretend success. Future write endpoints go through Store domain commands or document transactions and define undo, dirty, invalidation, and failure rollback together.
- File > Export provides Export As, Export All Frames, and Export Sprite Sheet. Export As opens the existing export settings. PSD is a Photoshop project format written by Save As or Export As and is not currently an open or import format. It writes only the current frame and full canvas while preserving layer and group hierarchy, names, visibility, opacity, supported blend modes, clipping relationships, locks, current-frame masks, and mappable layer styles. Text and Tilemap write as raster layers; it does not fabricate a Photoshop animation timeline. Export All Frames reuses existing format, scale, and export-directory settings, composites every timeline frame to a separate image in order, and names them with consecutive numbers; PSD and GIF are unavailable as per-frame batch formats. Export Sprite Sheet fully composites each frame, arranges frames left to right in timeline order, and opens an unsaved RGBA one-layer document for continued editing or ordinary export rather than writing directly to disk.

## Editing and History

- Editable text supports selecting characters and changing font size, line spacing, letter spacing, and color independently. When a selected range contains mixed values, the matching control is blank; a new value replaces the entire range. Font spacing uses literal glyph boxes and default line height. Actual spacing is calculated from final visible-pixel bounds; at `0px`, visible pixels of adjacent characters or lines touch directly.

### Tile Workflows

- Window > Tiled Preview offers Off, Surround, X-Axis, and Y-Axis. Surround shows a fixed 3 x 3 canvas, X-Axis 3 x 1, and Y-Axis 1 x 3, automatically zooming to fit every duplicate. Editing pointers on repeated copies map back to matching original-canvas coordinates. When selected content or a layer moves across an original boundary, the outside portion appears continuously in adjacent duplicates on enabled axes. Tiled preview belongs only to the current view, does not affect dirty state or undo history, and is not written to `.moonsprite`.
- New Tilemap Layer accepts layer name and tile width and height and is resizable. Tileset defaults to New Tileset but may select an existing project Tilemap Tileset. Selecting one adopts its tile dimensions automatically, creates blank cells with the same grid on every animation frame, and reuses that Tileset. Only no selection creates a project Tileset with an initial transparent tile. Creation opens the Tileset panel and enters Hybrid Edit by default. Multiple Tilemap layers may share a Tileset, and tile edits update every reference. While shared, the Tileset name no longer follows any individual layer rename; only a uniquely referenced Tileset follows its layer. The dropdown uses the Tileset name and selecting it also selects a representative layer. Deleting a layer removes the Tileset only with no other references, and undo restores both. Tilemap and free-tile share the Tileset panel. It auto-hides only when the current project contains neither type, and synchronizes visibility on project switch, delete, undo, and redo.
- Ordinary raster and background layers can use Convert to Tilemap Layer and the same size dialog. Confirmation crops every animation frame from a grid beginning at canvas origin, discards outside-canvas content, transparent-pads edge regions smaller than one unit, and reuses identical tiles across every frame of the current layer. Conversion keeps layer position, hierarchy, and display properties, removes background identity, selects the new Tileset, opens the panel, and enters Hybrid Edit. Conversion and rename form one undo step. Text layers and layers with enabled styles must first convert to ordinary layers.
- Tilemap has four session editing modes: In-Place Edit, Variant Creation, Hybrid Edit, and Paint Tiles. The first three appear in this order in a right-aligned icon segmented control. Paint mode uses a dedicated paint icon beside Delete. Tool options always show the current tool's own properties.
- Variant Creation allows all ordinary pixel tools and existing modifiers to modify a cell, then exactly reuses or creates the resulting tile and replaces only that cell reference. The original tile and other references remain unchanged. Hybrid Edit directly modifies the original tile in an occupied cell and refreshes all references; it exactly reuses or creates a result tile only after entering an empty cell and producing content. In-Place Edit allows pressing outside canvas or outside a valid tile and dragging into occupied tiles, and holding across multiple occupied cells. Each cell writes back to its corresponding tile and refreshes all references. A `Shift` line preview appears at every reference.
- If a Tileset still contains only its initial transparent tile, the first held stroke in In-Place Edit temporarily opens the whole Tilemap grid and allows one gesture across multiple cells. On release, retain the initial transparent tile and create or reuse a tile result for each actually changed cell. Selecting and moving content within one tile still edits that tile only, and pixels moved outside the unit are discarded.
- Paint Tiles fills complete cells with the active tile. Selection tools expand every touched region to complete tile cells. Moving selected content snaps by tile width and height and migrates cell references in real time without modifying Tileset pixels or creating tiles. Hover and drawing preview follow Pencil brush-preview settings and show a full tile as replacement rather than overlay. Transparent pixels and Eraser clear old cell content directly. Fast drag fills traversed cells inside the selection. Tileset left click sets foreground tile and right click background tile. Clicking any real tile immediately enters Paint Tiles. Canvas left and right buttons use foreground and background tiles. Eyedropper in Paint Tiles samples the tile reference under the cell. The bottom of the Color panel shows foreground and background tiles. One press-to-release gesture creates one undo step covering Tileset or cell references and selection position. The Tileset panel has no separate pixel editor.
- Moving selected content in Variant Creation always reuses or creates each moved cell's result without writing the original tile. In Hybrid Edit, translating a complete rectangular tile selection by whole-cell units migrates references only and creates or modifies no tile. Every other non-cell-aligned, partial, masked, or transformed move generates variants from results while keeping original tiles and other references unchanged.
- Tiles use the palette's square spacing, default outline, hover, and selected feedback. Each square shows one complete tile unit. Empty slots within the visible panel range are valid drop targets. Left click creates a temporary selection outline independent from foreground and background tiles. `Ctrl` toggles selection, `Shift` selects a contiguous range, and panel blur clears it. `Delete` or `Backspace` removes all temporary selected tiles; without temporary multi-selection it removes the foreground tile. Batch delete creates one undo. A Tileset always retains at least one tile, preserving the first when all are selected. `Ctrl+wheel` uses the same five sizes as the palette and remembers the result.
- Dragging begins only when the pointer hits a temporary selection-outline edge. One or multiple selected tiles retain their two-dimensional relative slot layout and can move to any empty slot or existing tile. Dropping into empty slots leaves originals empty. Dropping onto existing tiles fills vacated slots with displaced tiles. Slot layout is independent from the compact RGBA atlas and does not change stable tile IDs, canvas references, tile numbering, or final display. It saves as one undoable project operation. While `Ctrl` is held, non-empty tiles in the panel and canvas show indices from `0` in stable Tileset order. Canvas labels use a solid `#0000FF` background and white digits. Releasing hides all labels. Index hints are view state only.
- Moving an entire Tilemap layer with Move changes only offset coordinates of selected animation cels. Tileset, tile pixels, cell references, and grid dimensions remain unchanged and the tile grid follows layer offset. Behavior is identical in all four Tilemap modes and forms one coordinate-only undo like an ordinary layer move. Selection-content movement still follows the mode's cell or tile rules.
- Resizing canvas completes every Tilemap animation cel with full blank cells at the original grid phase so newly added canvas areas are immediately paintable. Keeping outside-canvas content retains original cells. Crop mode crops only complete tile cells and does not generate fractional raster caches. This structural change undoes, redoes, and saves with the canvas-size operation.
- Tilesets and Tilemap cells are project content. Layer copy, animation-cel copy, cross-project paste, and `Alt`-drag copy preserve Tilemap source data. Cross-project paste copies required Tilesets, establishes new layer ownership, and remaps stable references. Deleting or undoing paste also removes Tilesets introduced only by that pasted batch. Ordinary compositing, thumbnails, and image export continue reading the Tilemap raster cache, so exported output matches canvas display.
- New Free-Tile Layer accepts only a name and does not preset tile dimensions. Creation displays that layer's source-tile page in the shared Tileset panel, creates one transparent source tile automatically, and enters Edit Source Tile. First drawing on canvas automatically places an instance of that source and crops source dimensions to the operation's actual non-transparent content. To create another reusable item, click Add Source Tile and edit the new transparent source on canvas. The panel dropdown switches by free-tile layer and selecting an entry selects its layer. Switching active layer changes the same panel between ordinary Tilemap and free-tile source pages; there is no independent Free Tile Group panel.
- Every free-tile layer owns its own source collection. Each source stores pixels in an exclusive one-tile Tileset and has a name, display color, visibility, and lock state. Source data retains description, integer pixel offset, opacity, and blend-mode fields for old-project compatibility and internal positioning, but Source Properties shows only name, color, visibility, and lock and does not show an icon-and-size summary row. New free-tile layers and new sources receive a layer-color preset automatically. Double-click or right-click a source tile opens properties. Source and instance properties preview immediately during input. Close, backdrop, `Esc`, or `Enter` commits final values as one document-history step; dialogs show no Cancel or Save buttons. Show Tile Instances and Lock Tile Instances control every instance of the source and provide hover descriptions.
- Instances no longer appear below the Tileset panel. The icon at the right of a free-tile layer opens the layer panel's Instance Layers subview. If the active frame has no instances, the command stays in the ordinary layer view and shows a message. The subview has no independent timeline and lists only instances from the outer free-tile layer's active frame; frame switching, cel links, and animation are controlled by the outer layer. Each Place Instance adds one row, whose order is back-to-front compositing order. Rows provide ordinary-layer selection, visibility, lock, properties, drag reorder, context menu, and delete, and reuse the source's layer color marker.
- Ordinary click selects one instance row, `Ctrl` toggles, and `Shift` selects a contiguous range in display order. Opening properties from any multi-selected instance applies only fields actually changed in that session to all selected instances and creates one undo. `Delete`, `Backspace`, and context Delete remove every selected instance with one undo; if selection contains a locked instance, the whole batch is blocked. Instance eye and lock reuse ordinary layer gestures: press-drag across rows applies one state, leaving the range restores original values, and `Alt` click applies to every instance in the active frame, each as one undo. Instance Properties are ordered Blend Mode, Opacity, Rotation, Mirror, X, Y, with no extra title-information row. Context commands rotate clockwise `90°` or toggle horizontal or vertical mirror. These transforms affect only the instance, not the shared source; later source changes still synchronize through the instance transform. Show Only makes the current instance visible and hides every other active-frame instance as one history step.
- Clicking an instance selects it and enters Edit Source Tile automatically. Double-click does the same and collapses to single selection. Only inside the Instance Layers subview does Move select and move an individual unlocked instance. In the ordinary layer view, Move always selects and moves the entire free-tile layer. With an instance selected, source editing always uses that instance as the coordinate anchor, even in overlaps, while source pixels still refresh every same-source instance. Hidden instances do not composite or participate in canvas hit testing. Locked instances may change visibility but cannot move, reorder, transform, or delete. Source properties and pixels affect every source instance, while instance position, orientation, and depth remain independent. The outer free-tile layer retains ordinary layer position, visibility, lock, opacity, blend mode, grouping, and animation properties.
- Free-tile Place mode uses the current foreground or background source tile and places instances centered on the pointer pixel at arbitrary positions without Tilemap snapping; instances may overlap. Array order is back-to-front compositing order. Eraser deletes the topmost instance under the pointer. Eyedropper selects the source referenced by the topmost instance. Fast drag fills placement or erase along the pixel path. Full source preview follows brush-preview settings, and a transparent source still shows a visible outline. One press-to-release creates one instance-history entry; cancel restores all pre-press instances.
- Free-tile Source Edit mode uses Pencil or Eraser on any canvas instance to edit its source directly and also allows drawing from a never-placed new source. During preview, every instance of the same source across all frames refreshes in real time. With an instance selected, rectangle, ellipse, lasso, polygon lasso, and magic-wand selection retain only the portion inside that transformed instance. Moving, scaling, rotating, skewing, or copying selection content uses the instance as document-coordinate anchor, displays and operates on a temporary raster in the current instance's rotation and mirror orientation, then inverse-maps to the shared source on confirmation. Move preview refreshes on every pointer update without one-frame lag. Every same-source instance updates, while selection borders, pivot, and transform controls appear only on the active instance. One completed source-pixel transform and selection position form one undo step.
- If a source is erased to fully transparent, keep the current instance as an editable anchor. The next drawing can expand the source from that instance and must not lose its editing entry because transparent cropping reduced it to `1 x 1`. First source editing and first instance placement form one history step. Later confirmations produce one source-tile history entry. Cancel restores source image and any temporary placement. Source pixels, properties, instance position, orientation, and references are project content and persist through copy, paste, animation, and `.moonsprite`. Derived surfaces exist only for compositing, thumbnails, and export. Canvas resizing keeps arbitrary instance pixel coordinates and permits placement in new areas; crop removes only instances completely outside the new canvas. Image resizing scales all source dimensions, source offsets, instance coordinates, and caches by the same ratio.
- A canvas selection copied from an ordinary raster layer can paste into a free-tile layer. With no instance selected, paste crops transparent edges in original canvas coordinates, creates an independent source tile for every content block no larger than `256 x 256`, and places instances. With an instance selected, paste directly replaces that instance's shared source pixels, creates no source, Tileset, or instance, and refreshes every same-source instance. Each path forms one undo. Ordinary layer animation frames or cels do not convert to free tiles and cannot paste directly into frame cells of the Instance Layers subview. Selection inside Instance Layers owns `Delete` / `Backspace`, removes all selected instances at once, and does not delete the outer free-tile layer.

### Animation Timeline

- Animation uses an Aseprite-style layer/cel model. Every ordinary layer in the layer panel is a track across frames, and each frame cell to its right is that layer's cel for the corresponding frame. Layer name, visibility, lock, opacity, blend mode, grouping, and order are shared across frames; pixels, bitmap dimensions, and offset belong to the cel.
- Playback controls, frame add/delete/copy, frame duration, loop settings, and frame columns are integrated into the layer panel with no duplicate standalone timeline panel. Frame one begins directly beside the layer-name region. A draggable divider between names and frame columns persists its width. Frames run left to right and align strictly with layer rows. A selected frame is shown by one blue outer outline covering frame header and every cel row, with all four border sides continuous. The active-layer and active-frame intersection keeps only the dark active background and no extra inner blue box. Extra frames scroll horizontally in the same region.
- Layer Settings provides Hide Timeline. When enabled, all frames and cel data remain in the project, but the layer panel shows only the ordinary layer list. Timeline grid and layer-panel animation buttons disappear. Frame switching, playback, onion skinning, frame add/delete, matching shortcuts, and preview playback are disabled and shown as disabled. Turning it off restores the original timeline and data.
- Add Frame in the layer panel copies the current frame by default and creates independent cels. A frame-header context menu opens Frame Properties, copies the current frame, creates a blank frame with default `100 ms`, or deletes a frame. Blank, copied, or linked frames created inside a loop-section range remain in that loop. Creating from the loop's final frame extends the loop's right endpoint, combined with frame creation as one undo. Every frame stores and displays its own millisecond duration directly in the frame header; no global duration input appears in the panel toolbar. Animation always retains at least one frame.
- After selecting one or more frames, the frame-header context menu can create a named loop section. Loop sections render as independent bracket tracks along the lower edge of the layer-panel title bar, in the area containing playback controls. A bracket spans its start and end frame and displays its name on the line without occupying or embedding in frame headers. Contained loops nest by parent-child relationship, with children inside the parent bracket. Partially overlapping but non-contained ranges use outer tracks. Clicking a bracket selects its contiguous frames. Double-click opens properties. Context menu can play, edit, or delete it independently. Properties set name, start, end, forward or reverse direction, and finite or infinite repeat count. Independent loop playback uses frame durations and shared playback speed. Forward goes start to end, reverse end to start, and finite playback stops only after fully displaying its final terminal frame. Definitions save with the project and are undoable. Playback target and current repeat progress are session state only.
- Frame headers and cels use the same outer-outline multi-selection feedback and the same pointer-drag semantics as layers for sorting or moving across positions. After passing the drag threshold, every source becomes translucent; frames show an insertion position and cels a target cell. Release creates one undo. When frame-header selection exists, `Delete` or `Backspace` removes all selected frames. Batch delete creates one undo and retains at least one frame. Double-clicking a frame header opens frame-duration properties. Double-clicking a non-empty cel opens cell-opacity properties. Properties, Copy, and Delete are disabled for empty cels, while Paste remains available with cel clipboard content. Cell opacity reuses the layer-property slider, number input, and percentage layout. `Enter` commits and closes; `Esc` discards and closes. During playback, hide frame and cel selection outlines but retain selection state.
- Copy Frames copies duration and every layer cel for selected frames and pastes after the last selected frame. Duplicate pixels are independent. Copy Cels affects only selected cells. Context-menu Copy, Paste, and Delete labels must identify whether they target frames or cels.
- Layer and Preview panels share one playback state and one right-click settings menu. Speed options are `0.25x`, `0.5x`, `1x`, `1.5x`, `2x`, and `3x`, with Play Once or Loop and Return to Starting Frame on stop. Left click toggles play/pause. `Enter` toggles by default only when focus is not in an input, no modal dialog or menu is open, no selection or text transform is active, and no canvas operation owns `Enter`.
- With no selection, unmodified Left and Right arrows switch to previous and next frame. Comma and period switch frames whether or not a selection exists, preserve the canvas selection, and wrap at both ends. During playback, the first press of any frame-switch key stops on the current frame regardless of Return to Starting Frame, clears old frame or cell multi-selection, and retains only the current frame as a single selection. The next press after pause switches frames. Only one playback timer runs in the shared editor layer even when both panels are visible. Frame switching, speed, playback start frame, and return-on-stop are session state, do not affect dirty state, and do not enter history.
- While the pointer is over the layer panel, `Ctrl+wheel` switches and persists six display densities: Compact, Standard, Detailed, Expanded, Large, and Giant. Compact keeps buttons, layer name, and frames. Standard adds blend mode and opacity. Detailed and the three larger levels show thumbnails and progressively enlarge layer rows and frame cells. Cel thumbnails always use square containers filled completely with a transparency checkerboard. Visible content scales proportionally from its own bounds and centers, while pixels outside document bounds remain cropped. `Alt+wheel` scrolls the frame area horizontally. When the panel docks left or right, it must retain workable frame-area width. Ordinary painting does not generate thumbnails below Detailed density.
- Drawing, fill, and ordinary layer pixel operations modify only the active layer cel at the active frame. When a layer group itself is selected, fill tools and Fill with Foreground are unavailable. Tool rail, tool shortcut, and default `F` must not modify a child layer. Selection-content transforms may target all selected ordinary pixel layers or descendants of selected groups. With frame multi-selection, targets expand to every selected-layer and selected-frame combination. All targets share one selection outline while retaining separate layer pixels, order, and hierarchy and undo as one operation. If selection contains text, Tilemap, free-tile, hidden, or locked layers, block the combined transform. Creating, copying, and deleting a layer updates every frame track. Copying a layer copies all frame cels; deleting removes all corresponding cels.
- Text tool click creates auto-size text as before. Holding and dragging first previews a fixed region in real time; release opens Text Edit and the region remains visible before text input. Text wraps beyond region width and clips to height. Actual-spacing mode makes visible text touch the region edge. A selected fixed-region text layer continuously shows one region outline and eight controls. Text-tool hover inside shows Move. Click opens current text editing, press-drag moves region and text, and dragging controls changes region position or size with real-time rewrap. It does not scale glyphs or write raster transforms. Text-box outline and controls default to `#0000FF` and are configurable under Preferences canvas colors. Fixed-region text ignores `Ctrl+T`; auto-size text still uses `Ctrl+T` for ordinary layer scale, rotate, and skew.
- Fonts are selected in rows on the left. Import uses the component-library select and reads installed system fonts automatically. Select chooses a local font file and copies it into MoonSprite's `fonts` directory. The right configures multiline text, font size, line spacing, letter spacing, and jagged or smooth rendering. The input region stays compact. Live preview is on by default; disabling preview or cancel restores the original image, and confirmation creates one formal history entry. Confirmation creates a separate text layer at the click position and shows a text marker in its layer row. Every text cel stores editable parameters and a raster cache for compositing. Double-click a text layer or cel to edit again; double-click an empty text cel to create content. With a text layer selected, Text, Move, Eyedropper, Hand, Zoom, and Rotate remain available. Marquee, Pencil, shapes, Line, Airbrush, and Fill are disabled until conversion to an ordinary layer. Text cels copy, paste, or move only between text layers. Ordinary pixel tools and ordinary pixel paste cannot damage their editable source. Re-editing preserves completed transforms. Convert to Ordinary Layer retains the current raster result and deletes editable text data. Creation, editing, transform, and conversion are undoable and save with `.moonsprite`. Projects store only the font name, so another computer must install or import a matching font.
- A cel context menu can create or delete a Layer Mask. A layer or group row context menu creates corresponding masks in batch for every frame that already contains content. Groups use the name Layer Group Mask. Both are frame-specific. A new mask is an independent transparent grayscale surface: transparent means unpainted and acts as white, white fully shows, black fully hides, and gray proportionally reduces final owner alpha. Drawing any color converts it to opaque grayscale; Eraser restores transparent unpainted state.
- A mask appears as an independent track directly above its owner using ordinary layer row height, with cells using the same dimensions, spacing, and borders as ordinary cels. Detailed and larger densities show actual grayscale thumbnails. Ordinary click selects a mask cell and makes it the paint target, while the main canvas continues showing compositing with the mask applied. Cell highlight remains during editing, but transform borders stay visible only in the independent mask view. Holding `Alt` on hover uses the project-open pointer. `Alt+left click` enters independent mask view, and `Shift+Alt+left click` adds selection and enters the last clicked mask. In independent view, the main canvas becomes a white-background grayscale view showing only the mask; color picking, paint preview, and paste use that same grayscale view. Clicking any layer or group row, switching frame, or ordinarily selecting a cel exits mask editing.
- Mask cells support independent multi-select, copy, paste, link, unlink, and drag, with targets restricted to mask tracks and never rewriting ordinary cel pixels. Unlink preserves the currently resolved grayscale content. Copying a frame, layer, or group independently copies per-frame masks without sharing pixels with the original object.
- Add, copy, delete frame, change frame duration, and create, edit, or delete loop sections are undoable document edits. Frame switching, play, pause, playback position, playback range mode, active loop target, and repeat progress are session state and do not affect dirty state or undo. Undo from another frame must still modify the frame that owns the original operation.
- Playback strictly uses each saved frame duration and session speed. The playback button context menu offers Play Once, Loop All Frames, and Play Tag and Repeat. Play Once always runs first frame through last regardless of start and stops only after the final frame's full duration. Loop All returns from last to first. Tag Loop, when current frame lies inside one or more loop sections, selects the smallest innermost section and repeats it infinitely; outside every section it loops all frames. Tag Loop follows the section's forward or reverse direction but ignores finite repeat count. Clicking another frame during playback does not pause; tag looping switches immediately to that frame's innermost section and continues from the selected frame. Play Loop Section from a loop context menu strictly follows that section's own repeat count. With Return to Starting Frame enabled, stop returns to the frame selected before playback. With it disabled, completed Play Once returns to frame one, while manual pause stays at current frame. Drawing input is disabled during playback, but canvas pan and zoom remain available; editing resumes on pause.

- Document pixels, layer structure, layer properties, selection-content transforms, and adjustments are undoable edits.
- Canvas pan, zoom, view rotation, panel layout, and tool switching are not document edits and must not change dirty state.
- `Ctrl+Z` with nothing to undo must not mark the project unsaved.
- One user action should be one history step; for example, copying a layer and moving it is undone as a whole.
- `Delete` and `Backspace` act according to the last clicked or keyboard-focused canvas, layer, palette, Tileset, or brush-library region. `Ctrl+C` and `Ctrl+V` act according to canvas, layer, or palette focus. `Backspace` is equivalent to the available delete action while retaining normal text-input backspace. A legacy selection must not steal layer, palette, Tileset, or brush-library commands. Cross-project copy of one layer, multiple layers, or a group retains order, offset, properties, and parent-child group structure. Paste inserts directly above the uppermost selected object at the same level. Internal parent-child structure of copied groups remains. After paste, every pasted layer and group is selected. Batch paste or delete creates one undo.
- Copying a layer group also stores expanded or collapsed state. Paste restores it and selects the new group, nested groups, and every descendant layer so batch drag, delete, and properties immediately target the complete copy.

## Layers and Selections

- Root-level top and bottom drop targets use actual visible-row coordinates. Even when every visible row is a drag source, `Alt` copy to the very bottom remains possible. The bottom blue line sits directly below the last visible layer or group rather than at the panel-container bottom and shows triangle endpoints on both sides like an ordinary insertion line.
- Layer drag hover preview displays at most four visible selected objects as separate rows. Selected objects not shown individually or inside collapsed groups are summarized accurately as `+N`. Structural operations still process only top-level objects not already covered by a selected parent group.
- Layers and folders support nesting, unified ordering, drag in, and drag out. Every group uses a persistent ordering anchor, can sit between ordinary layers, and remains there after save and reopen. A new layer is inserted directly above the uppermost selected object at the same level. Grouping selected layers places the new group at the original position of the uppermost selected layer while preserving member order. With a layer inside a group selected, a new layer enters that group beside it. With a group itself selected, a new layer is created outside it at the same level; New Group creates an empty sibling group.
- Dragging above or below the layer panel places at the root top or bottom. Top and bottom blue lines and final drop use the same target even when the first or last item is a group and placement is outside it. The upper and lower quarters of a group title row create sibling insertion lines; only the middle shows a full-row blue drop-into-group box. Hitting a child row must not promote early to the parent box. This applies at every nesting depth, and the blue line's left edge aligns with final hierarchy. Dragging one selected group retains single-group selection and does not explicitly select descendants afterward.
- Layers and groups use the same selected background. Eye, lock, and folder have independent interaction regions and must not open properties accidentally. Press-dragging an eye or lock forms a dynamic continuous range from the pressed icon to the matching icon under the current pointer. Every object in range receives the state produced by the initial click; reversing and shrinking the range restores objects that leave it to their pre-gesture state. The whole gesture is one undo. An ordinary click ends on release; later hover must not keep changing state. `Alt+eye/lock` affects every layer and group at the current object's hierarchy level. An ordinary layer row shows the copy pointer only after an actual `Alt` drag begins; merely holding `Alt` does not change the pointer. The panel header retains only New Layer, New Group, and Delete; Merge and Ungroup are menu commands.
- An ordinary group at `100%` opacity uses pass-through compositing: members continue applying their blend modes one by one against lower content outside the group, so creating or removing the group does not change the image. Only when the group has a non-normal blend mode or opacity below `100%` is member content isolated and then composited to the outside through group properties. A group can independently enable Cumulative Blend: first composite members directly with outside lower content, then composite that complete result once more against the same outside background using group blend mode and opacity. This is off by default and saves as an undoable project property.
- Layers and groups can enable Clipping Mask from the context menu. Displayed content is limited by shape, pixel alpha, and object opacity of the immediately lower sibling at the same level. A clipping icon appears left of the name. Without a lower sibling, display remains unchanged and becomes active automatically after moving above a valid base. Clipping Mask is undoable and persists through copy, paste, and `.moonsprite`.
- Layer and group context menus provide Layer Styles with independently enabled Stroke, Shadow, Inner Glow, Color Overlay, and Gradient Overlay. The dialog title is `<Object Name> Layer Styles`; effect content does not repeat effect titles or descriptions. Stroke reuses ordinary stroke width, inside/outside position, round/square/horizontal/vertical quick shapes, and eight pixel-direction toggles and adds Both Sides.
- With Smart Hue enabled, Stroke hides the manual color field. Outer stroke reads the nearest visible source color per pixel; inner stroke reads the visible source color at the same position, then both darken through one RGB factor while retaining hue. With Smart Shadow enabled, Shadow likewise hides manual color and darkens the live background under the shadow through one RGB factor so shadow color follows background changes. Both darkness factors use `0%` for original color and `100%` for black. Gradient Overlay reuses smooth, Bayer, and directional dithering plus pixel preview from the Gradient tool.
- Layer styles are shared across frames. Group styles act on each frame's group composite. Both compute from the visible outline after the object's own mask and never rewrite source pixels. Shadow and outer stroke render behind content. Color Overlay, Gradient Overlay, Inner Glow, and inner stroke act inside content. Owner opacity, blend mode, and clipping mask apply afterward. Fixed and smart-generated style colors in indexed and grayscale documents resolve through current document color rules.
- Only objects retaining at least one style configuration show Enable Layer Styles or Disable Layer Styles in the context menu. Disabling temporarily removes the whole style set from compositing and output-boundary calculation without changing any child-effect parameters. Re-enabling restores original settings. Multi-selected objects toggle in one undo. The dialog supports live preview; Cancel restores opening state and Apply creates one undo. A clickable style icon remains at the right of a layer or group whenever it retains any style configuration, even while the whole set is disabled. Copy, paste, duplicate, and project save deep-copy style data and overall enabled state.
- A text layer or ordinary layer retaining style configuration can Convert to Ordinary Layer. Text-only conversion preserves existing raster surface and mask while removing editable text. With styles globally enabled, conversion bakes each frame's current style and mask result to independent pixels and removes styles, masks, and cel links. One undo restores either case completely.
- The layer context menu's Convert To submenu unifies Background Layer, Tilemap Layer, and Ordinary Layer. Ordinary Layer replaces the old Rasterize Layer command and removes background identity or bakes text, Tilemap, and layer-style results into ordinary pixels. An ordinary raster layer can convert to Background Layer and back. Conversion to background automatically leaves its group and moves to the absolute bottom of the root; identity and position changes form one undo. Text must first become an ordinary layer.
- The context menu's New submenu contains New Layer, New Layer Group, and New Background Layer, with Background fixed last. New Background opens a formal dialog with Solid Color, Grid, Stripes, Diamond 1, Diamond 2, or Circles. Solid Color is first and default, fixed initially at `#e4e4e4`. A new background is created at root bottom and shares the same preset content across existing animation frames by default. Background layer names always have an underline tight against the text and use an `image` pixel icon on the right.
- Background layers remain editable with ordinary pixel tools and participate normally in display and export. While an ordinary layer is selected, color picking treats every background layer as transparent and preserves the foreground's own alpha; selecting a background layer restores full-composite sampling. When any canvas edge expands, every active and inactive background-frame cel tiles the pre-expansion visible canvas once at the current anchor phase to fill the new canvas. Conversion does not repeat current content; later edits modify generated ordinary pixels directly. Creation, conversion, and canvas expansion are undoable. Background properties persist through copy, paste, and `.moonsprite`.
- Layer selection uses Photoshop-style modifiers. `Ctrl` toggles discontiguous layers and groups. `Shift` selects the contiguous visible-row range from the most recent selection anchor through the target in the complete current layer tree, including layers and groups. Mixed selected layers and groups form one operation unit for drag, `Alt` copy, delete, and context-menu batch editing of name, blend mode, opacity, display color, and description. Structural operations process only top-level objects not covered by a selected parent. Batch properties apply to every explicitly selected layer and group and must not omit a descendant merely because its parent is also selected.
- Drag, copy, and delete preserve top-to-bottom relative order and complete selection state for every top-level selected object and create one undo per user action. Drag hover preview lists every visible selected row in layer-panel order. Copy mode may use a source row itself as an insertion anchor above or below and show a blue line; ordinary move still rejects meaningless self-drops. After multi-selection, an ordinary click on any layer or group clears multi-selection and selects only that item. `Delete` removes all selected layers, groups, and their descendants and one undo restores them all.
- Single-object properties preview immediately. Batch-property form values update immediately while document preview coalesces to the last input at about 40 ms and forces final application when input stops or the panel closes. Batch properties overwrite only fields the user actually changed. Close, backdrop, or `Esc` keeps preview results and closes; the whole batch creates one undo.
- As long as the document contains a layer, the layer panel retains one valid selection and cannot enter a no-layer-selected state. Clicking blank layer-panel space clears multi-selection, group selection, and mask selection and returns to the active layer. Deselecting the last layer keeps it selected. Deleting the active layer selects an adjacent remaining layer. Undo and redo also preserve valid selection. Empty groups may be deleted. Locked layers, locked groups, and every descendant cannot change pixels, blend mode, opacity, structure, order, or deletion but may still edit name, display color, and description. Locking a group makes all descendants effectively locked and unable to unlock independently. An ancestor group containing any explicitly locked descendant cannot be deleted.
- Layer and group visibility, lock, and other content properties save normally with the project. Active layer, selected rows, selection anchor, and group expansion are remembered immediately by project path and restored after closing and reopening the application. Layer selection or expansion does not affect dirty or history. The next normal project save also carries the current layer-panel context. Invalid references are filtered and fall back to a valid active layer.
- Explicit frame and cel selections are mutually exclusive: selecting frames clears cel selection and selecting cels clears frame selection. Frame selection can coexist with layer or group multi-selection; selecting either side first and then the other must preserve the existing selection. Every selected-layer by selected-frame timeline cell displays derived combination highlighting without entering cel selection. Active layer and active frame still determine the current cel. Blank layer-panel space or ordinary operations outside the timeline clear matching selections by existing rules. Current-frame cells derived only from layer multi-selection show the layer-selection background without an extra blue cell outline; outlines appear only after explicit cross-frame cell selection. Press-drag range selection keeps a live temporary preview only for the active operation type. A frame-range drag entering cel rows continues expanding by the pointer's current column.
- Palette slots fill the panel's visible width and height adaptively. Expanding adds empty slots to the right and bottom. Shrinking removes only completely empty edges. If colors exist on the right or bottom, preserve their two-dimensional coordinates and enable component-library horizontal and vertical scrollbars without reflow. `Alt+wheel` scrolls horizontally. `Ctrl+wheel` switches Smaller, Small, Medium, Large, and Larger swatches at `22 / 30 / 40 / 52 / 64 px`; wheel up enlarges, down shrinks. Swatches have no gaps.
- Empty slots are clickable and valid drop targets and show no background box at default or hover. Every occupied swatch always draws a centered one-pixel theme-contrast border at the cell boundary. Adjacent colors share the border position so internal lines equal the outer border thickness. Light themes use a dark border; dark themes use a softer neutral border. Ordinary click replaces selection, `Ctrl` toggles discontiguous selection, and `Shift` selects a rectangular slot range. Clicking an empty slot uses the same selection outline and clears color selection. Leaving the whole palette clears color or empty-slot selection. Current selection shows a one-pixel dark inner line, three-pixel accent outline, and one-pixel dark outer line.
- Holding inside a cell for about 360 ms enters marquee selection. Moving while held updates the outline range in real time, even if movement began before the timer completed. Only a pointer on the accent-outline edge can drag the entire selected group. During group drag, the outline follows the preview and preserves its two-dimensional relative range. Dropping into empty slots leaves originals empty; dropping onto colors swaps with occupied slots. Double-clicking any slot adds the current foreground color and moves it there. Slot columns, positions, and empty locations are undoable project data and save with `.moonsprite`.
- Saving to MoonSprite always allows Save as New Palette. When current selection is a non-built-in user palette, Save to Current Palette is also available explicitly. Both retain columns, two-dimensional positions, and empty slots. Old compact palettes load in sequence. Swatches simultaneously indicate foreground with upper-right marker and background with lower-left marker; color roles themselves do not change with selection. Colors extracted from the current image or generated from PNG, Aseprite, and other external images sort by perceptual luminance from dark to light by default. Two-dimensional slot order saved in native `.moonsprite` remains unchanged.
- Foreground and background are shared across the application, not stored separately in project tabs. Switching, creating, or opening a project retains the two most recently selected colors.
- Layers and groups have no display color by default. The bottom of Properties offers No Color, a preset, or custom color. A set color appears only as a vertical strip at the far left of the row and does not change canvas pixels. Presets are managed globally under Preferences > Layers > Layer Property Color Presets and support one to twelve valid unique RGB colors. Applying Preferences updates every project's property panel immediately. Description uses the shared fixed-size multiline input and appears through the custom hover tooltip. Both are undoable layer properties saved with the project.
- A group's display color overrides the row marker of every descendant group and layer. Nested inheritance chooses the first set color from the outermost ancestor; once an outer group has a color, its entire subtree always shows it. An inner group or layer's own color is used only when every outer ancestor is unset. Inherited strips appear left of the eye like ordinary strips. Preset display color in Batch Properties is a discrete action: first click previews immediately rather than waiting for continuous-input coalescing.
- Hidden or locked layers cannot be drawn and the canvas shows a prohibited pointer.
- Selection may display outside canvas, but final pixel writes follow document- and layer-boundary data rules.
- Main tools, selection subtools, and shape subtools use one custom tooltip containing tool name, short usage, and the user's current shortcut. Main tool button and top options always show the selected subtool icon and name. Every subtool image remains in invisible DOM and decodes when the tool rail mounts so first selection never appears late. Selection popup is one fixed row: rectangle marquee, ellipse marquee, lasso, polygon lasso, magic wand. Polygon lasso defaults to `Shift+Q`. All share selection combination rules. Right button subtracts by default. Holding `Shift` when starting bypasses existing selection transforms and adds; tool options temporarily show Add or Subtract while held and restore the original mode on release.
- Close Lasso Selection Preview defaults off. Eyedropper remains active by default; only enabling Return to Pencil After Eyedropper makes a completed sample return on release. Temporary color picking is unaffected.
- Shape popup is one fixed row: rectangle outline, filled rectangle, ellipse outline, filled ellipse, free shape, polygon shape. Free Shape reuses the lasso path and creates a filled shape on release. Polygon Shape builds point by point and closes and fills by double-click, clicking start, or `Enter`. Fixed Ratio affects only rectangles and ellipses, uses width and height values, persists with at most one decimal place, and preserves that aspect ratio according to the primary axis for horizontal or vertical drag.
- Line is a main tool and Curve is in its two-item subtool popup. The main button always shows the selected line or curve icon. Line is drag-created and `Shift` constrains direction. Curve's first held drag previews the endpoint baseline in real time. After release, moving each control anchor changes curvature in real time; click fixes the current anchor, and completing the configured count commits. Anchor count is `1-8`, default `2`. Line and Curve share a separate line-brush preset with brush size, shape, texture, image brush, and brush mode. Perfect Pixel removes redundant pixels at path corners. Preview and final commit use the same path cleanup and brush-stamp rules. Both subtools have configurable shortcuts.
- Pencil, Eraser, five selection tools, six closed shapes, Line, Curve, and Paint Bucket share independent Horizontal, Vertical, and Diagonal checkboxes in tool options, which can be enabled together. Axes use canvas center and display blue guides. Multi-axis results deduplicate and one gesture creates one undo. Diagonal runs upper-left to lower-right and mirrored pixels outside a rectangular canvas are discarded.
- With Auto Select Layer enabled, Move hits an opaque pixel in another layer and moves that layer; transparent hit falls back to current selected layer. When a layer group itself is selected, Move shifts the group and all descendant current-frame content together and preserves group selection. `Shift`-clicking opaque content in another editable layer adds the hit cel at the active frame to existing cell selection without clearing cels selected in other layers or frames. Dragging any selected currently visible cel moves every selected cel by the same displacement and preserves timeline selection styling during movement. After multi-selection, ordinary click on selected or other layer content retains only the clicked layer and its active-frame cel. Clicking transparency returns to the active layer as a single selection. Directly dragging selected content must not clear multi-selection on pointer down.
- When no selection, shape, rotation, or other context owns it, holding Temporary Move Tool from Shortcut Settings, default `Ctrl`, performs the same behavior temporarily. Preferences can enable a brief content-bounds highlight when clicking a target layer.
- Edit > Adjust with a selection affects only the active layer selection; without one it affects every selected layer. Changing selection or layer selection while the dialog is open first restores the previous target baseline and recalculates preview on the new targets. Final confirmation creates one undo.
- Image > Resize Image detects the dominant integer pixel-block scale produced by nearest-neighbor enlargement in the final composite. Detection combines color-boundary periodicity and local same-color run lengths. Canvas dimensions need not be divisible by the scale and separate assets need not share grid phase. A small amount of text, details, or anomalous pixels outside the grid must not block dominant-scale detection. On success, show the multiplier, divide width and height by it and round to nearest integer, and switch interpolation to nearest neighbor. Do not guess for a solid canvas or when no reliable dominant multiplier exists.
- Hue/Saturation adjustment provides hue, saturation, and HSL lightness. Lightness `+100` converges to white and `-100` to black and must not reuse RGB brightness semantics.
- Pasted content and selection content after the first move remain floating until confirmation and can continue moving without early clipping to the viewport or overwriting traversed original background. Edit > Paste Special offers Paste as New Project and Paste as New Layer, with New Layer defaulting to `Ctrl+Shift+V`. Preserve source coordinates when they intersect the current visible view; otherwise place at current view center.
- Preview first caches artwork pixels with alpha and draws a transparency checkerboard independently at the final display scale. Like the main canvas, when checker cells fall below `2 CSS px`, it degrades to one light color and must not downsample high-frequency checker texture into artwork cache and produce stripes or moiré. The Preview title region provides Play/Pause left of Close. Left click plays loop or once by current settings; right click opens exactly the layer-panel playback menu.
- Timelapse Video provides frame-by-frame preview, playback controls, and progress seeking and exports records to user-selected MP4 or WebM. Export supports mutually exclusive target-duration and timelapse-multiplier modes. Video-size presets choose the largest integer pixel multiplier near the target range without stretching aspect ratio. If the codec requires even dimensions, pad at most one edge pixel. Output summary updates actual video size, record frame count, speed multiplier, and estimated duration. Export reuses the file-operation progress dialog; completion remains visually topmost until user confirmation. Before export, verify runtime video encoding support. Unsupported environments show failure and do not create a disguised format.

## Workspace and Settings

- Eight-direction resize for modal dialogs uses compact continuous hit bands centered on visible borders and must not cover nearby body controls. Dragging or resizing against the application edge retains pointer capture and pauses size-observer feedback without flicker or snap-back. During drag, update visual position directly and save only on release. Moving downward may place content below the window, but the complete title bar must remain reachable. Shortcut Settings category and body regions stretch with available height and the bottom action bar stays against the bottom.
- The user always has one active workspace and layout changes write to current state immediately.
- A workspace stores both creation-time layout and current layout. Reset restores only the creation-time layout.
- The default workspace cannot be deleted.
- Preferences and shortcuts persist after restart. Conflicting shortcuts show a clear warning. Importing a valid shortcut configuration shows success inside the dialog; read or parse failure shows an explicit error.
- Interface Scale means only MoonSprite's own scale. The desktop application compensates for current display system DPI. Changing Windows display scale or moving the window between displays with different DPI must not scale the actual visual interface size again. Main-window default and minimum dimensions use physical pixels and follow only MoonSprite interface scale, never an additional 125%, 150%, or 175% system DPI multiplier. Changing interface scale while maximized must not move the window or exit maximized state.
- The main window uses a fixed `32 px` Windows-native-looking title region with drag, double-click maximize, minimize, maximize/restore, and close and displays `MoonSprite` plus the current version. The title bar enters native drag only while the primary button remains down and the pointer actually moves. Clicking the title bar from another window merely to activate MoonSprite must not keep the mouse captured or move the window after release. After double-click maximize or restore, immediately restore the normal pointer rather than leaving the Windows top-border vertical-resize pointer. System DPI must not scale the title region again.
- MoonSprite software-cursor bitmap and hotspot coordinates use the same DPI compensation. System display scale must not change visual cursor size or hit position. Main-canvas percentage zoom is independent from interface scale. At canvas `100%`, one document pixel maps to one device pixel; setting interface scale to `150%` or another value must not create alternating widths, misalignment, or smooth sampling.
- Shortcut Settings export first opens the native JSON save dialog. After path confirmation, write UTF-8 atomically. Cancel creates no file. Write failure displays an error in the dialog.
- Numeric input accepts only digits, decimal point, spaces, scientific notation `e/E`, parentheses, and `+ - * /` expression characters. Typing or pasting other letters, Chinese text, or invalid symbols filters them. `Enter` or blur evaluates with standard precedence and then applies field minimum and maximum.
- Files preferences use one Autosave Recovery Data time preset. Recovery writes strictly at the selected interval and do not trigger additionally on window blur. Off registers no timed save.
- Preferences footer provides Apply, which saves current settings without closing. OK saves and closes. Cancel discards unapplied changes.
- Restore All Defaults requires confirmation and clears only MoonSprite local software configuration. It must not delete user-created projects, brushes, palettes, workspace files, or other disk files. The application reads translatable text through one language catalog and unknown language preferences fall back to Simplified Chinese. Preferences currently offers Simplified Chinese and English. Applying refreshes the current interface, Windows native file dialogs, and later core-operation messages immediately. User input and project content are never translated.
- A color-value button shows color sample, HEX, and color role. Fully opaque colors use six-digit HEX; colors with alpha use eight digits. Opening defaults to RGB and can switch RGB, HSV, HSL, and Gray. Mode, HEX, and current color share the top row. Every channel uses one colored track and direct right-side number input, and every mode supports alpha. Foreground/background controls at the bottom of the color wheel and Background Preferences reuse the same component.
- Foreground and background color-value dialog titles show Color Edit Foreground or Background and have a close button. Before movement, blur closes automatically. A real title drag makes it persistent. The two remember sizes independently, share minimum and maximum constraints, resize in eight directions, and may both be open. Reopening after close places each near its own trigger by default. Channel tracks adapt to body height and remain equal in height to value inputs. Default modes are HSV, RGB, LAB, GRAY, PLT; PLT selects directly in current project palette order. The right eyedropper enters temporary color pick and restores the original tool afterward.
- In Cursor Preferences, Use Local Cursor is off by default. When enabled, ordinary states prefer the computer's system cursor; operations without a matching system meaning, such as rotation, fall back to MoonSprite cursor, and cursor scale that applies only to MoonSprite cursors is disabled. When off, every state uses MoonSprite cursor. Tool Preview defaults to full brush preview. Show Brush Edge While Drawing appears only with Full Preview and Edge and controls whether the edge remains during painting. Showing a true mouse crosshair during marquee is independent and off by default, leaving only four corner marks around the current pixel.
- Background Preferences centrally controls transparency checker size with direct `1-256 px` input and two colors side by side. Layer Property Color Presets use the same ordinary color-setting layout and must not imitate an independent panel. Editing Preferences controls wheel zoom, pencil line live preview, Optimized Line Algorithm on by default, and diagonal line-step value. The same algorithm drives brush lines and polygon-lasso edges. `Shift+Ctrl` direction constraint provides 8 directions at step `1` and 12 above `1`. Default Zoom Mode controls whether zoom-tool drag uses smooth or percentage-step zoom.
- Size presets, export-scale presets, and layer property color presets in Preferences use the component-library `DeleteIconButton`. Compact preset rows use `28 px`. Color presets and their Delete buttons use `34 px` height with shared destructive hover and disabled states.
- When one form or dialog contains multiple text or number inputs, `Tab` and `Shift+Tab` move focus forward and backward in interface order and select all content in the newly focused input by default.
- Status messages appear in the upper canvas view below the top tool-options bar with clear separation and without covering view center. Adjustment, Canvas Size, and Image Size dialogs default midway between view center and right edge. Every modal uses one shared shell, can be dragged, resized from external eight-direction handles, and remembers its own position and size. Each dialog has a content-preserving minimum and viewport-appropriate maximum. The shell creates no horizontal or duplicate scrollbar. Default size is chosen not to crowd the canvas at 1080p.
- The bottom option area of Canvas Size places Sync All Sides to the right of Crop Content Outside Canvas. When enabled, editing any left, top, right, or bottom margin synchronizes the other three to the same value. Enabling the checkbox alone does not change current margins.
- Every project canvas has its own floating Quick Command Bar above it without consuming canvas layout height. It starts collapsed on first use; the user's last explicit expand or collapse action is saved as a local interface preference and reused for newly opened projects and the next application launch. When multiple split or floating project canvases are visible, each retains its own bar. Inactive canvases hide their expanded content temporarily without overwriting the saved state. The bar is translucent by default and Preferences can disable translucency. While translucent, hover or keyboard focus restores full opacity. The collapse button keeps fixed height before and after expansion. The command region uses horizontal reveal and opacity animation.
- Default Quick Commands are one row with horizontal and vertical selection mirror, horizontal and vertical canvas-view mirror, Invert Selection, Custom Grid, and Relative Brightness. Settings remains fixed at the end and opens the independent Quick Command Bar preference category. That category can hide the whole bar and can select and drag-order Undo, Redo, Select All, Deselect, Pixel Grid, Selection Borders, Relative Brightness, Reset View, Fill Foreground, Delete Selection Content, Swap Foreground and Background, Create Brush from Selection, and clockwise/counterclockwise view rotation. Quick commands and color-mode ordering reuse Home's continuous list, drag handle, and held feedback. Buttons with related settings can right-click directly to the matching dialog and state that in hover help. Selection commands are disabled without a selection. Canvas mirror and grid buttons show the owning project's state and call the same commands as top menus and shortcuts.
- Integer and fractional zoom use stable, consistent canvas geometry and sampling origin. Beginning or continuing drawing at a fractional scale such as 313% must not make existing pixels locally jump when the cached drawing path changes.
- After any top-level menu opens, moving horizontally into another top-level menu switches and opens it directly. Without an open menu, ordinary hover does not open menus.

## Compatibility and Safety

- Reject unknown `.moonsprite` format versions rather than guessing how to parse them.
- Save uses a temporary file and replacement strategy. Failure must not damage the original.
- Recovery, gallery, brush, and workspace directories are user data and are not committed to the source repository.

# Visual and Save Behavior

## View Mirroring

Horizontal and vertical mirroring change only observation, not canvas content or editing direction. The lower-left canvas indicator shows current mirror state. Input coordinates use the inverse display matrix.

When a mirrored canvas is zoomed to viewport edges, every surrounding pixel remains visible. It must not be clipped because the offscreen scene still uses pre-mirror bounds.

Preview-panel wheel zoom anchors at the pointer so the artwork position under the pointer remains stable. It uses the same percentage steps and pixel sampling as the main canvas and has no `125%` or `150%` step. Toolbar zoom buttons still anchor at preview center. With Follow Canvas Viewport enabled, Preview smoothly follows the location viewed in the main canvas while retaining its own zoom multiplier and zoom operations. Follow position is constrained so artwork edges do not pass beyond preview viewport edges. When artwork is smaller than the viewport it may move at most until the corresponding edge touches; when larger, it must not expose blank area outside artwork. Beginning manual pan in Preview continues from current follow position and disables Follow automatically. Turning Follow off directly also keeps the current preview position.

Window > Rotate View offers clockwise and counterclockwise 90° commands. Both can be bound in Shortcut Settings, change only the view, and do not enter document history.

## Saving

Normal Save, first Save, Save As, and Export display compact progress only after real encoding or writing begins and close automatically on success. Operations requiring a Windows file picker wait for it to return first. Canceling the picker shows no progress.

Saving an already opened `.moonsprite` re-encodes only changed cels, layers, masks, and project metadata that must refresh. Unchanged pixel blocks and timelapse snapshots reuse compressed data from the original file. Incremental save still atomically replaces through a temporary file in the same directory. If the original changes externally, reuse validation fails, or merge fails, fall back to full save without leaving an incomplete project.

Save As defaults to Preferences > Files > Default Save Format. Choosing PNG, JPEG, or WebP must explicitly warn that layers and project structure are not preserved. The confirmed extension, Windows save type, and actual encoded format must agree.

Export Settings always shows the current format suffix in the file name. Switching format preserves the base name and synchronizes the suffix. Each export can choose a directory independently, switch to the local gallery directory, or reuse the containing directory from a recently successful full export path. A directory selected in the dialog overrides the preference default export location.

Each project records its last export name, actual path, format, scale, target, GIF options, and selected preset only after a successful write and restores them when that same project reopens. Canceled or failed export must not overwrite the record. Export presets persist complete format, scale, GIF range and direction, file name, and directory settings. Reading an old preset must not discard export formats already supported.
