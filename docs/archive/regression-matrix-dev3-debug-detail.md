# DEV.3 Debug 回归明细归档

> 本文件保存 2026-08-07 精简前的完整场景快照，仅用于追溯历史 Debug。当前高风险契约以 `docs/testing/regression-matrix.md` 为准，普通开发不得加载本归档。

本文件只记录高风险用户工作流。曾复发、难以人工发现、共享核心算法，以及坐标、撤销、文件数据、持久化和平台安全 Bug 应补充场景与自动化测试；普通视觉、间距、文案和简单显隐问题由用户验收，不进入矩阵。状态使用“已有”“待补”“手动”。

| 领域 | 场景 | 预期 | 状态 |
| --- | --- | --- | --- |
| 历史 | 无历史时按 `Ctrl+Z` | 无副作用，dirty 不变化 | 核心测试与桌面 E2E |
| 历史 | Alt 复制图层后撤销 | 一次撤销删除复制层及移动 | 已有 Store 测试 |
| 多语言 | 缺失、损坏或尚未发布的语言偏好 | 启动、顶部菜单和首选项稳定回退到简体中文，不出现空键或半翻译入口 | `localization.test.ts`、`file-preferences.test.ts` |
| 多语言 | 首选项在简体中文与英语间切换 | 应用后当前界面、核心错误和后续撤销历史使用所选语言；已有工程名、图层名和用户输入保持原样 | `localization.test.ts`、`file-preferences.test.ts`、桌面人工检查 |
| 多语言 | 1024 × 640 下使用英语打开首选项 | 最长英文标签完整显示且不覆盖右侧下拉框、输入框或开关 | 1024 × 640 视觉检查 |
| 多语言 | 英语界面打开、保存、另存为、导出图片或导出色板 | Windows 原生文件对话框的筛选器使用英语，扩展名与所选格式保持一致 | `platform_dialogs.rs` 单元测试、桌面人工检查 |
| 视图 | 平移、缩放、旋转后撤销 | 视图不进入文档历史 | 旋转已有本机桌面 E2E，平移与缩放待补 |
| 视图 | 镜像视图、缩放工具拖动策略 | 镜像只改变显示；输入、缩放与平移仍使用未镜像文档方向 | `view-geometry.test.ts`、`canvas-input.test.ts` |
| 视图 | 切换镜像视图 | 合成画面立即水平镜像，文档像素和操作历史不改变 | 待补桌面场景 |
| 视图 | 分别开启、关闭水平和垂直镜像 | 提示文字与实际状态一致，垂直镜像快捷键和菜单均可用 | `view-geometry.test.ts`，待补桌面场景 |
| 视图 | 旋转或镜像后在指针位置缩放 | 缩放前后同一文档像素保持在指针下，旋转与两种镜像组合不回弹 | `view-geometry.test.ts` |
| 选区 | 角点、边中段和四角外侧命中 | 角点优先缩放，边中段独占，旋转区不与缩放区重叠 | `canvas-input.test.ts` |
| 视图 | 镜像后移动画笔、选区和形状预览 | 预览位置与视觉镜像后的落点一致，不残留在原位置 | 待补桌面场景 |
| 坐标 | 旋转视图后绘制和选区 | 像素、选区、预览位置一致 | 已有几何测试，待补 UI |
| 坐标 | 旧工程带图层偏移 | 复制、粘贴、变换不偏移 | 待补 |
| 剪贴板 | 系统剪贴板不可读或数据无效 | 回退到本软件最近一次选区复制内容，且复制来源不被粘贴修改 | `clipboard-service.test.ts` |
| 剪贴板/选区 | 框选后粘贴并切换 `Ctrl+H` | 只切换蚂蚁线显示，不生成残留像素；浮动粘贴仍可取消，`Ctrl+Z` 后文档恢复原状 | `workspace.test.ts`，待补桌面场景 |
| 剪贴板/选区 | 浮动选区镜像、移动后再次镜像 | `Shift+H`/`Shift+V` 始终基于当前浮动内容重新生成预览；移动后再次镜像不恢复旧方向或旧像素 | `workspace.test.ts`、`tools.test.ts` |
| 平台剪贴板 | 图像尺寸为零、溢出或超过限制 | 拒绝异常输入，不调用系统剪贴板 | Rust `platform_clipboard` 测试 |
| 指针 | 切换工具、按键松开或热更新后继续绘制/指向图层 | 指针立即更新且不叠加；画布和图层栏目都以最新指针事件校正丢失的 Alt 状态，不会误显示吸管或复制指针 | 工具切换已有桌面 E2E；`canvas-visuals.test.ts`、`canvas-input.test.ts`；指针图像待补 |
| 指针 | 重新切图后检查 32×32 热点 | 默认、绘制、移动、复制、吸管、缩放和旋转指针的实际操作点与热点契约一致 | 手动；热点坐标见 `pointer-modifiers.md` |
| 指针 | 隐藏或锁定图层绘制 | 禁止操作且无预览像素 | 待补 UI |
| 指针 | 指针命中选区缩放或旋转区域 | 工具像素预览隐藏，只显示当前变换指针 | 待补 UI |
| 选区 | 新建、加选、减选、交集 | 原蚂蚁线在操作中保持 | 已有算法测试，待补 UI |
| 选区 | 套索闭合后检查右侧和下侧边界 | 套索路径经过的像素属于最终选区，矩形路径的左、上、右、下四边均完整，不得固定缺少最右列或最下行 | `tools.test.ts` |
| 选区 | 魔棒按住预览并松开 | 预览区域与最终选区一致 | 已有算法测试，待补 UI |
| 选区 | 内容超出画布后移动回来 | 边界外像素完整保留 | 已有 Store 测试 |
| 选区 | 框选后切换到画笔 | 隐藏变换控制点和旋转命中，只保留蚂蚁线并限制在选区内绘制 | 待补桌面场景 |
| 变换 | 四角外侧旋转命中 | 在不显示蓝点的前提下提供角点附近 28 CSS px、互不重叠的紧凑旋转命中区 | `canvas-input.test.ts`，待补桌面场景 |
| 变换 | 八个缩放点与四角旋转区域命中 | 缩放点具有舒适且一致的命中范围；离开缩放区后连续进入最近角点的旋转区，不出现无响应空洞 | `canvas-input.test.ts`，待补桌面场景 |
| 变换 | 四角斜向缩放向外移动 | 斜向缩放命中区大于边中点命中区；越过命中边界后直接切换对应旋转指针，中间不出现移动选框或默认指针 | `canvas-input.test.ts`，待补桌面场景 |
| 变换 | 从四角斜向缩放点持续移入外侧 | 离开 5 CSS px 外侧缩放区后立即进入对应旋转指针；到角点 28 CSS px 后恢复普通套索输入，中间不得出现无响应空洞 | `canvas-input.test.ts` |
| 变换 | 从可见角点手柄移入外侧象限 | 可见 `8×8` 手柄及紧邻容差内仍为斜向缩放；越过手柄外缘后的第一个可用像素立即变为对应旋转指针，不得被 18px 内侧易点击范围继续占用 | `canvas-input.test.ts`，浏览器真实指针采样 |
| 浮动栏目 | 预览栏目八向调整尺寸 | 四条边与四个角的命中带贴合浮窗边框、互相连续，拖动方向与指针一致 | 手动，待补组件测试 |
| 浮动栏目 | 预览与其他栏目调整尺寸 | 所有悬浮栏目共用同一套边框内外对称命中带；预览不随视口比例单独缩放或把命中区压向内部 | 手动，待补组件测试 |
| 文件 | 另存为与导出进度提示 | Windows 文件选择器确认前不显示应用进度；确认路径后分别显示“正在另存为”“正在导出”，完成后自动关闭 | `workspace.test.ts` |
| 画布输入 | 拖拽完成、取消和修饰键切换 | 同一手势只完成一次；轴约束、缩放级别和选区整数缩放保持稳定 | `canvas-input.test.ts`，待补桌面 E2E |
| 画布输入 | 画笔已有直线锚点后执行翻转快捷键 | 直线预览立即清除，下一笔不连接翻转前的锚点 | 待补 Store/UI |
| 停靠布局 | 旧布局、重复栏目、栏目移动和最小尺寸 | 自动补齐栏目、保持唯一顺序并限制尺寸，移动前后不丢失布局 | `panel-layout.test.ts` |
| 笔刷 | 非基础笔刷调整尺寸 | 不闪烁基础笔刷预览 | 待补 UI |
| 笔刷 | 自定义笔刷三种模式 | 预览与实际平铺一致 | 已有工具测试 |
| 颜色 | 调色盘右键选择背景色 | 色相、滑条、选择点同步 | 已有颜色测试，待补 UI |
| 调色板 | 多选色块拖拽排序和尺寸预设 | 色块作为连续块移动，插入目标唯一，尺寸预设稳定 | `palette-layout.test.ts` |
| 调色板 | 恢复调色板栏目或旧尺寸偏好 | 色块尺寸至少使用稳定的最小格尺寸，不因布局恢复变成不可用的小方块 | `palette-layout.test.ts`，待补桌面场景 |
| 调色板 | 选中色块后按 `Delete` 或 `Backspace` | 删除全部选中色块并阻止同时删除图层或选区；操作可一次撤销 | `palette-layout.test.ts`、Store 测试，待补 UI |
| 调色板 | 左右键选择颜色格并失焦 | 左键设置前景色、右键设置背景色；被点击颜色格显示临时蓝色描边，失焦后只隐藏描边，前景/背景角标持续保留 | `palette-layout.test.ts`，待补 UI |
| 图层 | 文件夹嵌套和跨层拖放 | 插入线层级与结果一致 | `layer-panel-layout.test.ts`、`layer-operations.test.ts`，待补专用桌面场景 |
| 图层 | 多选图层后使用移动工具拖动 | 所有可编辑已选图层实时一起移动，并作为单一撤销事务 | 待补桌面场景 |
| 图层 | 修改显示颜色与描述后保存、重开工程 | 图层行色标和悬停描述保持；旧工程缺少字段时正常打开；修改作为一次属性历史可撤销 | `format.test.ts`，待补组件场景 |
| 快捷键 | 自定义椭圆选区、图像、视图、图层和修饰键 | 设置、菜单右侧提示、模式提示与实际执行使用同一命令定义 | `shortcuts.test.ts` |
| 快捷键 | 录入单独的 Ctrl、Alt 或 Shift | 保存为单个修饰键，不重复为 `Ctrl+Control`；允许清空绑定 | `shortcuts.test.ts` |
| 快捷键 | 快捷键长列表滚动 | 使用组件库统一滚动条，内容不超出设置窗口 | 待补 UI |
| 快捷键 | 重开设置后检查重复绑定 | 冲突状态由完整配置重新计算；先匹配命令正常使用，后续无法触发项显示警告图标和说明 | `shortcuts.test.ts`，待补 UI |
| 快捷键 | 直线连接模式开关 | 开关位于“快捷键设置 > 修饰键”，名称为“直线连接模式”，修改后画笔与橡皮擦的 `Shift` 连线立即同步 | `file-preferences.test.ts`，待补 UI |
| 快捷键 | 重新打开快捷键设置检查直线连接模式 | 显示当前快捷键，不显示独立开关；可清空或重新绑定 | `shortcuts.test.ts`，待补 UI |
| 弹窗 | Esc 关闭与图层属性 Enter 提交 | Esc 关闭当前弹窗；图层属性 Enter 保存并关闭 | 待补桌面场景 |
| 图层弹窗 | 打开、缩放和重置图层设置 | 弹窗使用窄而清晰的自适应排版，控件不重叠，底部操作栏贴底且无空白错位 | `LayersPanel.test.tsx`，1024 × 640 视觉检查 |
| 弹窗 | 同一表单存在多个文本或数值输入框 | `Tab` 按界面顺序切换到下一个输入框，`Shift+Tab` 反向切换；快捷键录入框仍负责录入快捷键 | `form-focus.test.ts` |
| 保存 | 保存过程反馈 | 保存时显示紧凑且可关闭的反馈；仅有实际阶段时更新进度，成功后自动关闭 | 待补桌面场景 |
| 保存 | 已有文件路径的普通保存 | 直接完成保存，不弹进度框；首次保存或另存为才显示紧凑反馈 | 待补 Store/UI |
| 保存 | 尚未落盘工程执行普通保存 | 直接调用系统保存流程，不先显示 MoonSprite 保存进度框 | 待补 Store/UI |
| 视图 | 水平或垂直镜像后绘制、吸色、缩放和平移 | 显示位置与鼠标操作位置一致；文档像素不被镜像修改；左下角持续显示当前镜像轴 | `view-geometry.test.ts`，待补 UI |
| 视图 | 镜像视图放大至画布铺满视口 | 离屏场景覆盖镜像后全部可见区域，画布四边不被裁切 | `canvas-render-plan.test.ts` |
| 图像 | 调整画布尺寸时预览画布外图层 | 新画布边界包含的原画布外像素在裁剪预览中可见，提交结果与预览一致 | 待补核心/UI |
| 图像 | 调整画布大小弹窗显示四个边界输入 | `Left/Top/Right/Bottom` 使用统一标签轨道，两行输入框与步进按钮按列对齐 | 1024 × 640 视觉检查 |
| 图像 | 扩大画布时预览原画布外图层 | 图层像素可见，但旧画布外、新画布内区域统一叠加黑色遮罩 | `canvas-resize-preview.test.ts` |
| 图像/动画 | 多帧工程调整画布尺寸 | 所有帧和 cel 的内容随同一画布偏移保持原位置；扩大、裁切、撤销和重做结果一致 | `animation.test.ts`、`workspace.test.ts` |
| 图像/画笔 | 调整画布后在新增区域移动画笔 | 不落笔也能立即显示完整笔刷预览，不沿用旧画布边界或旧坐标 | `canvas-input.test.ts`，待补桌面场景 |
| 调整 | RGB 与单通道曲线 | RGB 主曲线后依次应用 R/G/B 贝塞尔曲线；单通道不改变其他通道 | `adjustments.test.ts` |
| 调整 | 曲线直方图 | 按当前活动图层基线统计 RGB、R、G、B 分布，透明像素不计入，直方图不修改文档 | `adjustments.test.ts` |
| 调整 | 曲线通道切换与控制点删除 | 通道按钮有明确色彩倾向；中间点可稳定删除，端点不可删除 | 待补组件场景 |
| 变换 | 缩放手柄跨过对侧边界 | 继续缩放并切换对应水平/垂直镜像；预览与最终提交一致且一次撤销完成 | `canvas-input.test.ts`、`tools.test.ts` |
| 变换 | 边或角点跨过对侧边界 | 对称轴取被拖动的边；斜向拖动以被拖动角点为轴交点 | `canvas-input.test.ts`、`selection.test.ts` |
| 动画格式 | v1 工程打开、v2 保存、无效帧和 cel | 旧工程变为单帧时间轴，未知版本拒绝，cel 关系不重复 | `animation.test.ts`、`project-format.test.ts` |
| 工作区 | 调整、切换、复位、重启 | 当前与初始布局语义正确 | 待补 Store/UI |
| 工作区 | 显示、隐藏、停靠、浮动和调整栏目 | 颜色、调色板、图层和预览四个栏目独立保存可见性与布局；旧工作区中的独立时间轴配置被忽略，隐藏栏目不占停靠空间且可从“窗口”菜单恢复 | `workspace-layout-preferences.test.ts`，待补桌面场景 |
| 文件 | 拖入 `.moonsprite`、`.ase`、`.aseprite` 或图片 | 统一规范化 Windows 与 `file://` 路径，直接打开并进入编辑器 | `document-drop.test.ts`，待补 Tauri E2E |
| 文件 | 另存为不同格式 | 扩展名、系统类型和编码一致 | 已有格式测试，待补 E2E |
| 启动 | 文件关联启动工程 | 不停留首页，不重复弹窗 | `test:tauri`、`test:desktop` |
| 关闭 | 连续点击关闭并取消 | 同一时间只出现一次确认，取消后旧超时不得退出软件 | 已有 Rust 状态测试，待补桌面 E2E |
| 恢复 | 自动保存进行中放弃恢复 | 等待写入结束后删除，已放弃草稿不得被晚到写入重新创建 | 已有恢复服务测试 |
| 恢复 | 从首页打开恢复工程 | 正常进入编辑器并可操作，不出现黑屏或无活动会话 | 待补 Store/UI |
| 启动恢复容错 | 恢复目录或会话标记不可写时启动软件 | 恢复辅助数据记录警告但不阻止主窗口启动，编辑器仍可正常操作 | `platform_recovery.rs`、桌面启动检查 |
| 新建 | 点击“创建画布”进入编辑器 | 复用已缓存的资源信息并预加载画布模块，不因重复系统内存探测停留在首页数秒 | `resource-info-cache.test.ts`，待补桌面计时 |
| 新建 | 打开新建画布弹窗且剪贴板含图片 | 只读取图片头部尺寸，不跨进程传输整张 RGBA；宽高立即更新 | `NewDocumentDialog.test.tsx`、Rust `platform_clipboard` 测试 |
| 输入 | 带单位的长数值输入框从右向左拖选 | 输入框空白区域仍属于文字编辑区，可跨单位左侧选择任意数字片段 | 待补组件测试 |
| 选区 | 八个变换手柄在边界两侧命中 | 使用连续文档坐标，左右与上下命中距离对称，不向右下偏移 | `view-geometry.test.ts`、`canvas-input.test.ts` |
| 选区 | 选框边缘移动与四角外侧旋转 | 边缘内外侧优先移动选框；离开边缘带后连续进入最近四角旋转区，光标与按下操作一致 | `canvas-input.test.ts`，待补桌面场景 |
| 选区 | 套索工具在已有选区四角附近继续框选 | 斜向缩放与旋转只占用角点附近的紧凑区域；越过旋转区后立即恢复套索输入，周边像素可继续选择 | `canvas-input.test.ts` |
| 变换 | 旋转选区时按住 `Shift` | 旋转角度吸附到 0°、45°、90° 等八个方向；松开 `Shift` 恢复连续角度 | `canvas-input.test.ts` |
| 变换 | 斜向缩放时分别按住 `Shift`、`Ctrl` 或两者 | `Shift` 固定宽高比，`Ctrl` 使用整数硬边缘倍数，两者组合时按原比例进行整数倍缩放 | `canvas-input.test.ts` |
| 撤销 | 滚轮缩放预览尚未提交时立即执行 Undo/Redo | 先同步提交当前视图预览，再撤销或重做文档操作；缩放、平移和旋转均保持不变 | `workspace.test.ts`、`view-preview-lifecycle.test.ts` |
| 文件 | 将 `.moonsprite` 从资源管理器拖入软件 | 普通 Windows 路径和 `file://` 路径统一规范化、去重并打开工程，不被重复拖入事件吞掉 | `document-drop.test.ts`，待补桌面 E2E |
| 文件 | 从资源管理器拖入 `.moonsprite`、`.ase`、`.aseprite` 或受支持图片 | Tauri 官方窗口拖放事件作为主入口，Drop 路径只提交一次并直接进入编辑器 | `document-drop-events.test.ts`、`document-drop.test.ts`，待补桌面 E2E |
| 文件 | 拖入监听中有单个通道订阅失败、组件重挂载或重复上报 | HTML、Webview、Window 与 Rust 通道互为兜底；任一通道可独立工作，重复路径只打开一次，清理后可重新订阅 | `document-drop-service.test.ts`、待补桌面 E2E |
| 预览 | 在预览窗口内将鼠标停在作品局部并滚轮缩放 | 缩放前后的同一作品像素保持在鼠标下，不再固定以预览中心放大 | `preview-geometry.test.ts` |
| 指针 | 拖动选区的缩放或旋转变换 | 按下拖拽期间统一显示移动指针，松开后恢复命中区域对应指针 | `canvas-visuals.test.ts`，待补桌面场景 |
| 变换 | 已通过 `Ctrl` 复制出的浮动选区再次按住 `Ctrl` 拖动 | 先提交当前副本，再从当前位置创建新副本；可连续重复，原内容与此前副本保持不变 | `canvas-input.test.ts`、Store 测试 |
| 输入 | 当前表面支持 `Delete` 删除时按 `Backspace` | 非文本输入状态下执行相同的选区、图层、图层组或调色板删除；文本输入仍正常退格 | `command-context.test.ts`、Store 测试 |
| 工具栏 | 从子工具弹出栏选择选区或形状工具 | 主工具按钮在同一交互帧切换到对应图标、名称和说明；所有图标在工具栏挂载时已解码 | `EditorToolRail.test.tsx` |
| 组件库 | 首选项预设和图层颜色预设的删除操作 | 使用组件库统一删除图标按钮；默认、悬浮和禁用状态一致，图层颜色按钮与删除按钮等高 | `DeleteIconButton.test.tsx`、`PreferencesDialog.test.tsx` |
| 菜单 | 点击打开任一顶部菜单后横向移动鼠标 | 无需再次点击即可切换并展开鼠标当前所在的其他顶部菜单 | `menu-behavior.test.ts` |
| 菜单 | 打开“文件 > 最近打开文件” | 子栏列出全部保留的最近工程与图片，名称始终包含真实后缀，点击后使用统一文件打开路径 | `home-history.test.ts`，待补 UI |
| 视图 | 从“窗口 > 旋转视图”执行 90° 旋转 | 顺时针与逆时针分别改变视图 90°，不切换工具、不写入文档历史，且可绑定或清空快捷键 | `shortcuts.test.ts`，待补 UI |
| 输入 | 在任意统一数值输入框输入 `2+3*4`、括号或负数 | 回车或失焦后安全计算表达式，再执行字段范围限制；非法表达式与除零恢复原值 | `numeric-expression.test.ts`、`NumberInput.test.tsx` |
| 首选项 | 修改自动恢复时间、光标、笔刷预览、透明背景和编辑设置 | 旧设置兼容读取；确定后实时应用，重启后保持；关闭自动恢复时不执行定时或失焦写入 | `file-preferences.test.ts`，待补 UI |
| 首选项 | 首次打开光标与工具预览设置 | 默认使用 MoonSprite 指针和完整笔刷预览；开启本地指针时禁用光标比例；“绘制时显示笔刷边缘”只在完整预览并显示边缘时出现 | `file-preferences.test.ts`、`PreferencesDialog.test.tsx` |
| 首选项 | 点击“应用”保存设置 | 设置写入本地但首选项窗口保持打开；再次点击“确定”才关闭 | `PreferencesDialog.test.tsx` |
| 颜色 | 打开颜色值按钮并切换 RGB、HSV、HSL、Gray | 默认 RGB；每种模式有透明度、HEX 输入和滑块，修改结果同步到对应颜色 | `color-values.test.ts`、`ColorValueControl.test.tsx` |
| 背景 | 修改透明棋盘格大小与两种颜色 | 主画布、画布尺寸预览、作品预览和笔刷预览使用同一配置，重置恢复中等默认值 | `canvas-visuals.test.ts`，待补 UI |
| 颜色 | 按下前后景色交换快捷键 | 默认 `X` 一次交换前景色与背景色，并可在快捷键设置中修改或清空 | `shortcuts.test.ts`、`workspace.test.ts` |
| 快捷键 | 打开曲线与色相/饱和度并自定义其他菜单操作 | `Ctrl+M` 打开曲线，`Ctrl+U` 打开色相/饱和度；其余已注册命令默认留空且可在快捷键设置中绑定 | `shortcuts.test.ts`，待补 UI |
| 快捷键 | 执行新建图层 | 默认 `Shift+N` 新建图层，菜单提示和快捷键设置显示同一绑定，用户可修改或清空 | `shortcuts.test.ts` |
| 颜色 | 打开颜色值编辑器调整任意通道 | 预览条和滑块轨道显示对应颜色变化，数值输入位于滑块右侧且修改结果一致 | `ColorValueControl.test.tsx`，待补视觉检查 |
| 颜色 | 未移动的前景/背景颜色弹窗修改 HEX 后点击外部 | 输入框失焦时立即提交颜色并同步调色板对应色块，不因临时弹窗先关闭而丢失提交 | `ColorValueControl.test.tsx`、`workspace.test.ts` |
| 图层 | 新建图层并设置显示颜色与描述 | 新图层默认无色标；设置预设色或自定义色后仅在图层行最左侧显示竖条；描述使用统一多行输入并通过自定义提示展示 | `document.test.ts`、组件测试，待补桌面视觉检查 |
| 缩放 | 缩放工具按住 `Shift` 后拖动 | 不进入抓手平移；本次拖动临时按百分比级数缩放，松开后恢复首选项中的默认缩放模式 | `canvas-input.test.ts` |
| 调整 | 曲线弹窗通道、删除和重置按钮 | 按钮在窄视口内保持同高、居中且不挤压曲线编辑区 | `AdjustmentDialog.test.tsx`，1024 x 640 视觉验证 |
| 性能 | 128、512、1024 画布平移、缩放、旋转后缩放和连续绘制 | 固定输入下输出帧分位数、>25ms 比例、主绘制与指针处理耗时；视图操作不进入撤销历史 | `scripts/canvas-performance.mjs`、`canvas-render-plan.test.ts`、`canvas-composite-cache.test.ts` |
| 性能工具 | 统一数值输入框支持表达式后运行完整画布基准 | 按“画布宽度/画布高度”的稳定可访问名称填写尺寸，不依赖输入框内部的 `type` 实现 | `pnpm bench:canvas -- --full` |
| 安装 | 全新用户安装与首次启动 | 无黑屏、无重复弹窗 | 发布前手动；桌面脚本使用隔离应用数据验证启动路径 |
| 首页 | 画廊工程读取失败后点击项目 | 只重试打开并保留磁盘文件；移除记录与删除文件必须是两个明确操作，删除文件必须二次确认 | `HomeWorkspace.test.tsx` |
| 首页 | 最近和画廊包含多个大型工程 | 文件名列表立即出现，缩略图最多 3 项并发加载并按路径与时间复用缓存；单项失败不阻塞其他项目 | `HomeWorkspace.test.tsx` |
| 弹窗 | 调整实时预览或图层属性修改后按 `Esc` | 调整弹窗取消时恢复打开前状态；图层属性始终实时保存，关闭或 `Esc` 只隐藏属性面板 | `AdjustmentDialog.test.tsx`、`LayersPanel.test.tsx` |
| 删除 | 画布仍有选区时在图层或调色板按 `Delete` | 根据最后操作区域删除图层、完整图层组、颜色或选区内容，不受遗留选区干扰；拒绝删除最后一层时不产生 dirty 或历史 | `command-context.test.ts`、`workspace.test.ts` |
| 首选项 | 点击恢复所有初始设置 | 二次确认后只清除软件本地配置，不删除工程、笔刷、色板、工作区或其他用户文件 | `PreferencesDialog.test.tsx` |
| 保存 | 首选项修改默认保存格式后打开另存为 | 弹窗默认选中配置格式；图片格式明确提示不保留工程结构 | `SaveAsDialog.test.tsx` |
| 笔刷 | 切换三种自定义笔刷模式 | 来源对齐、目标对齐和独立盖章的说明、原始多色预览与实际绘制语义一致，并使用统一下拉组件 | `tools.test.ts`、工具属性栏组件检查 |
| 剪贴板 | 在图层栏目复制单层、多层或图层组并跨项目粘贴 | 保留顺序、偏移、可见性、锁定、透明度、混合模式、显示颜色、描述和嵌套组结构；一次撤销删除整批粘贴内容 | `clipboard-service.test.ts`、`workspace.test.ts` |
| 图层 | 空组、非空组、普通层和嵌套组混合排序并跨层拖拽 | 所有组遵守持久锚点并可与图层自由排序；新建项置顶；插入线与最终目标层级左侧对齐；拖出栏目顶部或底部分别显示并落到首尾 | `layer-panel-layout.test.ts`、`layer-operations.test.ts`、`workspace.test.ts` |
| 图层 | 在眼睛、锁或组文件夹交互区单击或双击 | 只执行对应显示、锁定或折叠操作，不启动拖动、不打开图层属性 | `LayersPanel.test.tsx` |
| 图层 | 依次点击首层与末层并使用 `Ctrl` 或 `Shift` | `Ctrl` 离散增减选；`Shift` 从选择锚点到目标按可见图层行连续范围选择，行为与 Photoshop 一致 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 选区 | 已有非矩形选区时命中移动选框 | 只在真实蚂蚁线边界附近进入移动选框，不按包围矩形的空白边命中 | `canvas-input.test.ts` |
| 选区 | 套索预览闭合偏好与多边形套索 | 自由套索按偏好显示闭合预览；多边形套索逐点建立边界并可闭合、取消 | `tools.test.ts`、`PreferencesDialog.test.tsx`，待补桌面场景 |
| 选区 | 已有选区内按住 `Shift` 开始新选区，或使用右键拖动 | `Shift` 绕过变换命中并执行组合选择；右键默认减选，不被内容移动或变换抢占 | `canvas-input.test.ts`，待补桌面场景 |
| 形状 | 绘制实心/空心矩形和椭圆并设置固定比例 | 空心模式只绘制边界；固定比例时拖动结果始终符合长短轴比例 | `tools.test.ts`、`canvas-input.test.ts` |
| 调整 | 调整弹窗打开期间修改选区或多选图层 | 有选区时动态作用选区；无选区时动态作用全部选中图层；目标变化后实时预览重新基于原始数据计算 | `workspace.test.ts`、`AdjustmentDialog.test.tsx` |
| 输入 | 使用 `Tab` 切换数值输入并提交四则表达式 | 新输入框内容自动全选；表达式计算结果立即提交并触发实际修改 | `form-focus.test.ts`、`NumberInput.test.tsx` |
| 新建工程 | 使用默认尺寸预设打开新建画布弹窗 | 默认弹窗宽度足以让六个内置尺寸保持同一行，输入框、颜色模式和底部操作不被压缩 | `NewDocumentDialog.test.tsx`、1024 × 640 视觉检查 |
| 移动 | 自动选择图层开启时从透明位置拖动 | 命中其他图层内容时移动该层；透明位置回退移动当前选中图层，多选时整体移动 | `workspace.test.ts`、`canvas-input.test.ts`，待补桌面场景 |
| 弹窗 | 打开状态提示、调整、画布大小和图像大小弹窗 | 状态提示位于视图上方；调整类弹窗位于视图中心与右侧边缘之间的中点；弹窗默认紧凑、可调整大小并记忆用户尺寸 | `ModalShell.test.tsx`、各弹窗组件测试，待补桌面视觉检查 |
| 剪贴板 | 从“编辑 > 选择性粘贴”执行新项目或新图层 | 新项目创建独立文档；新图层使用 `Ctrl+Shift+V`，原坐标在当前可见视图内时保持原位，否则居中到可见视图 | `workspace.test.ts`、菜单组件测试 |
| 选区 | 自由套索或多边形套索连续输入并在已有选区内部按住 `Shift` | 输入过程不阻塞界面；内部加选实时显示组合预览，提交后结果与预览一致 | `canvas-input.test.ts`、`tools.test.ts` |
| 形状 | 开启固定比例并输入两个比例值 | 参数显示为“固定比例”，两个数值共同定义宽高比，矩形与椭圆预览和提交都遵守比例 | `tools.test.ts`、工具属性栏组件测试 |
| 弹窗 | 打开、拖动和八向缩放不同内容量的弹窗 | 默认位于视图中心与最右侧之间的中点；把手使用边框外侧命中且不被弹窗外壳裁掉，事件不得穿透到画布；尺寸限制在各弹窗合理最小/最大范围，内部只有必要的内容滚动条 | `ModalShell.test.tsx`、1024 × 640 与 1920 × 1080 视觉检查 |
| 渲染 | 在 313% 等非整数缩放下连续落笔 | 提交像素后画布 CSS 几何、取样原点和已绘制像素保持稳定，不出现局部像素抖动 | `canvas-render-plan.test.ts`、`canvas-visuals.test.ts`，待补桌面场景 |
| 弹窗 | 首次或升级后打开画布大小、图像大小和编辑调整弹窗 | 弹窗中心落在视图中心与视图右边缘之间的中点；八向缩放至允许最小尺寸时外壳和正文均不出现横向或纵向滚动条 | `ModalShell.test.tsx`、1024 x 640 与 1920 x 1080 视觉检查 |
| 偏好 | 首次打开“首选项 > 编辑” | 显示“套索选区预览闭合”且默认关闭；“吸管取色后切回铅笔”默认关闭并可持久化 | `file-preferences.test.ts`、`PreferencesDialog.test.tsx` |
| 图层 | 多选图层后点击图层栏目空白 | 清除多选和组选择，但保留当前活动图层以便继续编辑 | `LayersPanel.test.tsx`、`workspace.test.ts` |
| 图层 | 把图层拖入组、拖到栏目顶部或底部 | 蓝色落点线按最终父组层级缩进；顶部和底部安全带分别落到根层首位和末位，最上方为组时仍可放到组上方 | `layer-panel-layout.test.ts`、`LayersPanel.test.tsx` |
| 图层 | 选中空组后按 `Delete`，或尝试编辑、拖动、删除锁定层与锁定组 | 空组可作为一次历史删除；锁定对象及锁定组后代拒绝内容修改、结构移动和删除，同时保留解锁入口 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 快捷键 | 依次按住多个修饰键和普通键录入组合 | 修饰键按下期间持续显示完整组合，普通键按下后一次保存；单独修饰键在松开或失焦时保存 | `shortcuts.test.ts`、`ShortcutDialog` 组件场景 |
| 选区 | 新建模式下在已有选区外单击或在同一文档像素内拖动 | 单击取消选区；发生实际 CSS 像素拖动时建立 1 像素选区，不因起止文档坐标相同而误判为单击 | `canvas-input.test.ts`、待补桌面场景 |
| 选区 | 在已有选区内部使用加选、减选或交集 | 组合选择不被内容移动或变换命中抢占；套索拖动期间仍显示实际作用像素预览 | `canvas-input.test.ts`、`tools.test.ts` |
| 移动 | 存在选区时用移动工具移动单层或多层 | 图层内容和选区按同一位移同步移动，并由一次撤销完整恢复 | `workspace.test.ts`、待补桌面场景 |
| 形状 | 固定比例开启后点击交换按钮 | 宽高比例值立即互换，预览和后续绘制使用交换后的比例 | 工具属性栏组件测试、`canvas-input.test.ts` |
| 调整 | 色相/饱和度中调整“明度” | 使用 HSL 明度独立调整；`+100` 趋向白色、`-100` 趋向黑色，不与亮度/对比度的 RGB 亮度混用 | `adjustments.test.ts`、`AdjustmentDialog.test.tsx` |
| 颜色 | 打开前景色或背景色编辑弹窗后拖动标题栏并关闭 | 弹窗可在视口内拖动，右上角关闭按钮立即关闭；重新打开时从对应颜色按钮附近重新定位 | `ColorValueControl` 组件场景 |
| 渲染 | Windows 4K 与非整数 DPR 下显示画笔、橡皮擦和选区预览 | 所有相邻预览像素共享设备像素边界，内部不出现白线、描边或接缝 | `canvas-visuals.test.ts`、DPR 1.25/1.5/2 桌面检查 |
| 渲染 | 半透明图层参与合成时在上层跨越 128px tile 边界绘制 | gutter 仅参与缓存，不重复合成到主画布；绘制中和提交后均无横竖区块边缘闪烁 | `canvas-composite-cache.test.ts`、完整画布性能场景 |
| 提示 | 普通模式和高级模式显示状态提示 | 提示位于工具属性栏下方的画布视图上部，不覆盖顶部属性栏 | 1024 x 640 与 1920 x 1080 视觉检查 |
| 指针 | 选区工具悬停在任意文档像素 | 当前像素格四角显示紧凑直角角标，角标随缩放和视图变换贴合该像素，不与变换指针叠加 | `canvas-visuals.test.ts`、待补桌面场景 |
| 选区 | 新建模式在画布内外单击，或在同一像素格内真实拖动 | 单击只取消已有选区且松开后保持选区指针；没有选区时单击为空操作；画布外不夹取到边缘；超过 CSS 阈值的同格拖动建立 1 像素选区 | `canvas-input.test.ts`、待补桌面场景 |
| 选区 | 已有选区内使用减选或交集 | 保持选区角标指针和像素预览，不出现移动、缩放或旋转指针 | `canvas-input.test.ts`、`canvas-visuals.test.ts` |
| 选区 | 执行反选和隐藏蚂蚁线 | 反选得到画布范围内当前选区的补集并可撤销；`Ctrl+H` 只切换蚂蚁线显示，不删除选区、不进入文档历史 | `workspace.test.ts`、`shortcuts.test.ts` |
| 提示 | 状态提示出现在编辑器中 | 提示与顶部工具属性栏之间保留明确间距，不覆盖属性栏 | 1024 x 640 与 1920 x 1080 视觉检查 |
| 颜色 | 同时打开前景色和背景色编辑器 | 两个独立弹窗均可常驻、拖动和关闭，标题与关闭图标对齐；修改各自颜色互不关闭或覆盖另一个实例 | `ColorValueControl.test.tsx`、待补视觉检查 |
| 调整 | 连续拖动调整弹窗滑块 | 实时预览每帧最多重算一次，拖动保持连贯；松开或应用时使用最后数值，撤销仍只有一个条目 | `AdjustmentDialog.test.tsx`、定向交互性能检查 |
| 图层 | 把任意图层或组拖到栏目顶部 | 顶部蓝线显示并落在根层首位；首项是组时仍可放在组上方；拖动结束后蓝线立即清除 | `layer-panel-layout.test.ts`、`LayersPanel.test.tsx` |
| 图层 | 没有选中图层时新建组 | 创建空组，不自动收纳最上方图层 | `workspace.test.ts` |
| 图层 | 直接选中组或选中组内图层时新建图层 | 直接选中组时新图层与该组同级；选中组内图层时进入该组，且都位于目标容器顶部 | `workspace.test.ts` |
| 图层 | 子层或子组锁定后删除祖先组 | 拒绝删除包含任意锁定后代的祖先组；解锁后才允许删除 | `workspace.test.ts`、`layer-operations.test.ts` |
| 图层 | 祖先组锁定后尝试操作后代锁图标 | 所有后代有效锁定且不能单独解锁；悬浮提示明确显示锁定祖先组名称 | `LayersPanel.test.tsx`、`workspace.test.ts` |
| 画布大小 | 半透明图层存在时打开调整画布大小预览 | 预览合成保持图层原透明度，与打开前画面一致，不因预览缓存重复合成而变亮 | `canvas-composite-cache.test.ts`、待补桌面场景 |
| 快捷键 | 录入 `Ctrl+Shift+V` 等被系统或其他软件注册的组合 | 弹窗在捕获阶段阻止浏览器和应用命令，只记录完整组合，不被全局快捷键处理截断为修饰键 | `ShortcutDialog.test.tsx` |
| 颜色 | 在项目 A 修改前景色或背景色后切换项目 B | 所有打开工程立即使用同一前景色和背景色；旧工程颜色字段仍可兼容读取但不再造成切换回退 | `workspace.test.ts`、调色盘组件检查 |
| 选区描边 | 在描边弹窗按下 `Enter` | 只提交一次描边并立即关闭弹窗，不被全局快捷键或输入控件吞掉 | `OutlineDialog.test.tsx` |
| 选区 | 框选拖到画布视口边缘，或隐藏蚂蚁线 | 拖动期间视图连续自动平移且不进入文档历史；隐藏后蚂蚁线和八个变换点全部消失，选区本身仍有效 | `canvas-input.test.ts`、`canvas-visuals.test.ts`、待补桌面场景 |
| 选区 | 首次移动选区内容后继续往返移动 | 首次移动建立稳定源缓存，背景只恢复一次，后续位移不会把预览结果重新采入缓存 | `tools.test.ts`、待补桌面场景 |
| 填充 | 非矩形选区内使用连续或不连续油漆桶 | 只修改选区 mask 内像素，并正确处理图层偏移和透明区域 | `tools.test.ts` |
| 选区 | 已有选区时使用加选、减选或交集模式 | 加选模式在选区内部可移动内容；减选和交集仍执行选区运算；各模式不会整体丢失变换与预览能力 | `canvas-input.test.ts`、待补桌面场景 |
| 指针 | 使用框选工具悬停画布像素并切换十字指针偏好 | 默认只显示四角角标；开启偏好后叠加十字，设置可持久化 | `file-preferences.test.ts`、`canvas-visuals.test.ts` |
| 颜色弹窗 | 打开前景色或背景色编辑器并在标题栏拖动、缩放 | 标题明确显示颜色角色；未移动时失焦关闭，发生真实移动后常驻；八向缩放、尺寸记忆和内部高度自适应与统一弹窗一致 | `ColorValueControl.test.tsx`、待补视觉检查 |
| 图层 | 复制嵌套图层组后选中另一对象并粘贴 | 粘贴组位于当前对象的父级并与其同层，复制组内部的子组和图层层级保持不变 | `clipboard-service.test.ts`、`workspace.test.ts` |
| 图层 | 多选后普通单击任意图层或组 | 清除原多选并只选择被点击项；`Ctrl` 和 `Shift` 的多选规则保持不变 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 图层属性 | 编辑已锁定图层或图层组 | 名称、显示颜色和描述可实时保存；混合模式和不透明度禁用且 Store 拒绝绕过修改 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 图层 | 最末项为图层组时把根图层拖到栏目底部 | 底部安全带显示根层蓝线并把图层放到组及其成员之后；组与图层共用一致的根层边缘规则 | `layer-operations.test.ts`、`LayersPanel.test.tsx` |
| 选区指针 | 新建/加选时按住 `Shift`，或悬停变换命中区 | 默认只显示当前像素四角角标；偏好开启后显示鼠标十字指针；变换命中区不叠加角标 | `canvas-visuals.test.ts`、待补桌面场景 |
| 选区轮廓 | `Ctrl+H` 隐藏选区轮廓 | 只隐藏蚂蚁线，八个变换点继续显示并可命中 | 待补渲染器测试 |
| 选区描边 | 描边弹窗内按 `Enter` | 描边写入活动图层并生成历史记录，然后弹窗关闭 | `OutlineDialog.test.tsx` |
| 选区浮动 | 同一选区内容连续移动到已有像素上再移开 | 从第一次移动起复用原始内容和背景快照，目标背景在后续移动时完整恢复，直到确认或取消选区 | `tools.test.ts`、`workspace.test.ts` |
| 画布合成 | 非整数缩放、半透明图层和连续绘制 | 最终画面通过单张连续表面输出，不出现 128 px 分块边界或重复透明度 | `canvas-composite-cache.test.ts`、待补 4K 桌面场景 |
| 颜色弹窗 | 缩放颜色编辑器、关闭后重新打开 | 通道滑条与数值框等高；尺寸受美观上下限约束；每次打开恢复触发按钮附近的默认位置 | `ColorValueControl.test.tsx` |
| 图层组粘贴 | 复制组后选中另一对象并粘贴 | 粘贴组位于当前选中对象的父级，与当前对象同层；内部嵌套结构保持 | `workspace.test.ts` |
| 图层组锁定 | 查看未锁定与锁定组 | 未锁定显示解锁图标，锁定或继承锁定显示锁定图标 | `LayersPanel.test.tsx` |
| 选区指针 | 在任意缩放和高 DPI 下悬停画布内外 | 四角角标使用固定 CSS 线宽，视觉范围不小于 `8 x 8 px`；画布外仍显示四角角标而不是禁止指针 | `canvas-visuals.test.ts`、待补桌面场景 |
| 选区描边 | 描边弹窗打开时按 `Enter` | 键盘事件只提交描边弹窗，不触发全局“完成选区”，描边写入后选区保持有效 | `command-context.test.ts`、`OutlineDialog.test.tsx` |
| 图层多选 | 从图层行按 `Shift` 范围选择到图层组，随后按 `Delete` | 范围内图层与组都显示选中；组及后代和直接选中图层一次删除、一次撤销恢复 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 图层粘贴 | 分别粘贴图层组、单图层到组/图层/空白选择 | 组粘贴到当前对象父级；单图层按组内、图层同级、根层规则放置；所有新对象位于目标层级最顶部 | `workspace.test.ts` |
| 多边形套索 | 连续单击顶点后点击起点、双击末点、按 `Enter` 或 `Esc` | 点击起点、双击或 `Enter` 闭合并提交，`Esc` 取消；移动指针时末段预览实时跟随 | `canvas-input.test.ts`、待补桌面场景 |
| 直线算法 | 开启“直线算法优化”后以 `Shift` 连接两点或绘制多边形套索边界 | 主轴像素被平均分配到各阶梯，最长与最短阶梯相差不超过 1；画笔、多边形套索的预览与最终结果使用同一点列 | `pixel-line.test.ts`、`canvas-input.test.ts`、`tools.test.ts`、`file-preferences.test.ts` |
| 选区浮动 | 移动选区内容后，在未确认的当前位置按住 `Ctrl` 再次拖动 | 先确认上一段移动，再以当前位置为来源创建副本并移动；原位置内容保留，复制拖动可继续调整 | `canvas-input.test.ts`、`workspace.test.ts` |
| 选区描边 | 通过 `Shift+O` 打开描边且焦点仍在画布时按 `Enter` | 描边弹窗接管按键并应用一次，选区保持；成功使用的颜色、宽度、位置、方向、形状和预览开关随工程保存 | `OutlineDialog.test.tsx`、`project-format.test.ts` |
| 多边形套索 | 逐点单击任意斜向顶点并闭合 | 已确认顶点之间显示真实直线，不生成水平/垂直矩形折线；点击起点、双击或 `Enter` 均可正常闭合 | `canvas-input.test.ts` |
| 图层分组 | 多选不连续图层后新建组 | 新组占据所选图层中最上方图层的原显示位置，组内保持原图层顺序，未选图层相对顺序不变 | `layer-operations.test.ts` |
| 图层组粘贴 | 粘贴包含嵌套组和多个图层的集合 | 所有新组和新图层同时显示选中，撤销恢复原选择，重做恢复完整粘贴选择 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 选区创建 | 从画布内容边界外按住并拖入画布 | 拖动期间只在有效画布内显示像素路径，不在画布外绘制预览；松开前不显示新选区蚂蚁线，最终掩码限制在有效画布 | `canvas-visuals.test.ts`、待补桌面场景 |
| 选区创建 | 使用矩形或椭圆框选并按住 `Shift` 拖动 | 预览和提交均保持等宽高，不因选区模式或拖动方向失效 | `canvas-input.test.ts`、待补桌面场景 |
| 多边形套索 | 连续建立至少三个顶点后双击 | 无论浏览器 `PointerEvent.detail` 是否可靠，双击均闭合并提交；开启直线算法优化时所有边使用均匀阶梯 | `canvas-input.test.ts`、待补桌面场景 |
| 工具栏 | 展开选区工具与形状工具 | 选区五项和形状四项始终单行、顺序与命名固定；悬浮提示包含名称、简短用法和当前快捷键 | `editor-tools.test.ts`、待补 UI |
| 形状比例 | 输入超过一位小数的固定宽高比例 | 保存到工具状态的宽高比例均四舍五入到一位小数，界面不重新出现冗长小数 | `workspace.test.ts` |
| 预览 | 大尺寸透明画布缩小到预览栏目 | 棋盘格与主画布一样按最终显示比例绘制，格子过小时退化为统一浅色，不把高频棋盘纹理预合成进缩略缓存 | `preview-geometry.test.ts` |
| 多边形套索 | 未闭合时按中键或 `Space` 拖动画布 | 平移只改变视图；已建立的顶点、组合模式和原选区保持，松开后继续建立多边形 | `canvas-input.test.ts` |
| 选区创建 | 已有选区时按 `Shift` 加选或右键减选 | 按下和拖动期间旧蚂蚁线持续显示；新路径松开后才合并显示，未拖动的单击只在新建模式取消选区 | `canvas-input.test.ts`、待补桌面场景 |
| 工具栏 | 悬停任意主工具、选区子工具或形状子工具 | 自定义提示统一显示工具名称、简短用法和用户当前快捷键；形状工具使用指针源工程第 5 行第 3 至 6 列图标 | `editor-tools.test.ts`、待补 1024 x 640 视觉检查 |
| 图层属性 | 在首选项中新增、修改、删除或恢复显示颜色预设 | 预设限制为 1 至 12 个有效且不重复的 RGB 色，应用后所有工程的图层属性弹窗实时使用同一组预设 | `file-preferences.test.ts`、`PreferencesDialog.test.tsx` |
| 动画 | 在图层栏目新增空白帧并在同一图层绘制不同内容 | 图层树保持不变；每帧 cel 像素独立，来回切帧不串帧、不改变 dirty，也不产生撤销步骤 | `animation.test.ts`、`workspace.test.ts`、`LayersPanel.test.tsx` |
| 动画 | 复制帧后修改副本，再删除当前帧 | 复制帧继承全部图层 cel 但像素独立；删除后选择相邻帧，至少保留一帧且一次撤销恢复完整帧 | `animation.test.ts`、`workspace.test.ts` |
| 动画 | 在帧 A 绘制，切到帧 B 后执行撤销与重做 | 历史记录始终修改帧 A 的 cel，不误改当前帧 B；切回帧 A 后可见撤销/重做结果 | `history.test.ts`、`workspace.test.ts` |
| 动画 | 调整帧时长并循环/单次播放 | 播放按每帧毫秒时长推进；循环在末帧回到首帧，单次播放在末帧停止；播放状态不进入文档历史 | `animation.test.ts`、`LayersPanel.test.tsx` |
| 动画文件 | 保存并重开含多帧、多图层 cel 的 `.moonsprite` | v2 容器在 `cels/` 保存独立位图，活动帧、帧时长和循环状态完整恢复；旧 v1/v2 单帧工程继续打开 | `project-format.test.ts` |
| Aseprite 动画 | 导入或另存为包含多帧与不同持续时间的 `.ase/.aseprite` | 每个 Aseprite frame/cel 映射到相同图层轨道；多帧像素、顺序和持续时间往返保持 | `aseprite.test.ts` |
| 动画图层 | 新建、复制或删除图层后逐帧检查图层栏目帧列 | 新层当前帧创建空白 cel；复制层复制全部帧 cel 且像素独立；删除层移除全部对应 cel | `animation.test.ts`、`workspace.test.ts` |
| 动画图层栏 | 调整图层名称区宽度并在栏目内按住 `Ctrl` 滚轮 | 第一帧始终紧邻名称区左侧；名称区宽度可拖动并持久化；显示密度在紧凑、标准、详细三档切换，详细档显示图层信息与缩略图 | `LayersPanel.test.tsx`、待补桌面视觉检查 |
| 动画帧菜单 | 右键播放按钮或任意帧头 | 播放菜单可设置速度、单次/循环和停止返回起始帧；帧菜单可打开帧属性、复制当前帧、新建空白帧和删除帧；每帧时长互相独立且默认 `100 ms` | `LayersPanel.test.tsx`、`workspace.test.ts` |
| 动画视图 | 播放动画时连续滚轮缩放画布 | 切帧重绘不取消尚待提交的缩放预览；缩放持续响应且不进入文档撤销历史 | `useCanvasViewPreview.test.tsx`、待补桌面场景 |
| 动画单元格 | 跨图层、跨帧多选 cel 后复制粘贴或拖到另一单元格 | 以用户选中或抓住的 cel 为锚点，完整保留多选块的行列相对位置；目标越界时整次操作不执行，不折叠到同一帧、不清空未移动源内容 | `workspace.test.ts`、待补桌面拖拽场景 |
| 动画单元格连接 | 连接多个 cel 后编辑、断开和撤销 | 同一图层的连接 cel 共享像素表面并在相邻网格中显示为连续块；断开后目标 cel 拥有独立表面，修改不影响源 cel，连接与断开均可撤销/重做 | `animation.test.ts`、`workspace.test.ts`、`LayersPanel.test.tsx` |
| 动画帧排序 | 单选或多选帧头后拖到另一帧前后 | 被拖帧连同全部图层 cel 和时长按原相对顺序移动，活动帧及多选状态保持，操作只产生一个撤销步骤 | `workspace.test.ts`、`LayersPanel.test.tsx`、待补桌面拖拽场景 |
| 动画帧剪贴板 | 选中一个或多个帧后复制并粘贴 | 完整复制帧时长与全部图层 cel，粘贴到最末选中帧之后并选中新帧；副本像素独立，撤销一次删除整批副本 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 动画单次播放 | 在首帧或末帧启动“播放一次”，并开启停止后返回 | 始终从第一帧播放到最后一帧，末帧按自身时长完整显示后停止，再返回播放前选中的帧；播放期间不显示帧或 cel 选择描边 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 动画属性弹窗 | 双击帧头或有内容 cel 后按 `Enter`、`Esc` | 两类对象均打开对应属性；`Enter` 保存并关闭，`Esc` 放弃本次表单值并关闭 | `LayersPanel.test.tsx` |
| 动画栏目停靠 | 图层栏目在底部拉宽名称区并横向滚动后停靠到左侧或右侧 | 名称区按当前栏目宽度动态收敛，帧头和 cel 行仍完整对齐且可横向滚动；`Alt+滚轮`横向移动帧区，`Ctrl+滚轮`可进入超大显示档 | `LayersPanel.test.tsx`、待补桌面停靠视觉检查 |
| 动画帧选择 | 单击或多选帧头 | 蓝色外描边覆盖帧头及该帧的完整 cel 列，不使用单独表头描边，不在右上角产生断线或重叠色块；播放期间隐藏列描边 | `LayersPanel.test.tsx`、待补桌面视觉检查 |
| 动画缩略图 | 在非方形画布和含画布外像素的 cel 上切换图层栏六档显示密度 | 详细及更大三档始终显示方形缩略图，缩略图按完整画布边界取景而非按非透明内容裁切，画布外内容不挤占取景 | `animation-thumbnail.test.ts`、`LayersPanel.test.tsx` |
| 动画单次播放回退 | 关闭“停止时返回开始帧”后播放一次 | 始终完整播放首帧至末帧，自动完成后回到第一帧；人工暂停仍停在当前帧 | `workspace.test.ts`、`useAnimationPlaybackClock.test.tsx` |
| 预览栏动画 | 点击预览栏播放按钮或右键打开播放设置 | 预览栏拥有独立播放时钟、速度、单次/循环、停止返回设置和当前帧；不会修改图层栏或画布播放状态，视图缩放仍可用 | `PreviewPanel.test.tsx` |
| 动画单元格属性 | 双击 cel 调整不透明度 | 不透明度使用与图层属性一致的滑块、数值输入和百分比布局；`Enter` 保存关闭，`Esc` 放弃关闭 | `LayersPanel.test.tsx` |
| 动画拖拽 | 按住帧头或有内容 cel 移动到另一位置 | 使用与图层一致的指针拖拽；超过阈值后全部源对象半透明，帧显示插入线、cel 显示目标格，松手后一次撤销完成移动 | `LayersPanel.test.tsx`、`workspace.test.ts` |
| 动画选择互斥 | 先选择帧或多个 cel，再点击图层行 | 清除帧列和 cel 的显式多选，只保留当前图层与活动帧交叉 cel 的会话活动态，不出现内部蓝框 | `LayersPanel.test.tsx`、`workspace.test.ts` |
| 动画空单元格菜单 | 右键无可见内容的 cel | 属性、复制和删除不可用；已有 cel 剪贴板时仍允许粘贴到空格 | `LayersPanel.test.tsx` |
| 动画单元格连接清理 | 清空或删除已连接 cel，并尝试连接空 cel | 清空目标立即断开链接；同组其他 cel 保留独立像素；不存在可见内容的 cel 不允许连接 | `animation.test.ts`、`workspace.test.ts`、`LayersPanel.test.tsx` |
| 动画菜单边界 | 在图层栏底部或右侧右键 cel | 菜单根据实际尺寸在窗口内重定位，不被窗口边缘或任务栏遮挡；菜单项仍可操作 | `LayersPanel.test.tsx`、待补桌面视觉检查 |
| 动画缩略图取景 | 非方形画布或大画布只有小块可见内容 | 方形缩略图全部区域使用透明棋盘格；可见内容等比放大到合适尺寸并居中，仍裁掉文档边界外像素 | `animation-thumbnail.test.ts`、待补桌面视觉检查 |
| 动画网格缩放 | `Ctrl+滚轮` 放大到最大显示密度后纵向滚动到底部 | 最后一行 cel 下方保留适当滚动余量，缩略图和选中框不会紧贴或被底部滚动条裁切 | `LayersPanel.test.tsx`、待补桌面视觉检查 |
| 动画网格层级 | 选中帧或 cel 后同时进行横向、纵向滚动 | cel 内容和选中描边始终位于 sticky 帧头与图层名称区下方；帧列外框顶部完整且名称区不会被覆盖 | `LayersPanel.test.tsx`、待补桌面滚动场景 |
| 动画栏目停靠 | 图层栏目在底部、左侧和右侧之间切换并滚动帧区域 | 图层名称分隔线、帧头和 cel 网格使用同一列宽基准，首帧不产生空列或横向错位 | `LayersPanel.test.tsx`、待补桌面停靠视觉检查 |
| 动画帧与单元格多选 | 分别使用 `Ctrl` 点击切换和 `Shift` 点击范围选择帧或 cel | `Ctrl` 保留并切换离散对象；`Shift` 从锚点选择连续帧或矩形 cel 范围；点击 cel 激活所属图层但不提前清空动画选择 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 动画选择失焦 | 帧或 cel 出现蓝色选框后点击图层栏空白、画布或其他栏目 | 清除显式帧/cel 蓝框；当前活动帧和当前 cel 会话状态保留 | `LayersPanel.test.tsx` |
| 动画拖拽指针 | 长按帧头或有内容 cel 并超过拖拽阈值 | 拖动源半透明，指针立即切换为移动指针，结束或取消后恢复 | `LayersPanel.test.tsx`、待补桌面指针场景 |
| 动画缩略图边界 | cel 内容被自动放大并居中显示 | 只显示方形棋盘格和像素内容，不额外绘制会穿过像素的画布边界线 | `animation-thumbnail.test.ts`、`LayersPanel.test.tsx` |
| 可缩放弹窗操作栏 | 向下拉高未来计划及其他带底部按钮的 `ModalShell` 弹窗 | 正文占用剩余高度，底部操作栏始终贴住弹窗下边缘，不在操作栏下方留下空白 | `ModalShell.test.tsx`、`FutureRoadmapDialog.test.tsx` |

| 工具栏 | 切换选区或形状子工具后查看主按钮与顶部属性栏 | 主按钮立即显示当前子工具图标，名称、说明和快捷键均来自当前子工具；所有子工具图标在弹出菜单首次打开前已预解码 | `editor-tools.test.ts`、待补 UI 场景 |
| 多边形套索 | 未闭合多边形时按中键或 `Space` 平移画布 | 平移期间与松开后都持续显示已建立边和指针末段，平移只改变视图且不清空顶点 | `canvas-input.test.ts`、待补桌面场景 |
| 选区模式 | 选区工具中按住 `Shift` 或鼠标右键 | 属性栏临时跳转到加选或减选反馈，松开后恢复持久模式；实际组合规则与可见反馈一致 | 待补 UI 场景 |
| 文件拖入 | 将 `.moonsprite`、`.ase`、`.aseprite` 或图片拖入桌面窗口 | Webview 与 Window 原生拖放通道任一可用即打开文件，单一通道订阅失败不影响另一通道 | `document-drop-events.test.ts`、`document-drop.test.ts` |
| 光标首选项 | 切换“使用本地指针”并进入旋转等特殊交互 | 开启时普通场景使用系统指针、系统没有对应指针时回退 MoonSprite 指针；关闭时全部使用 MoonSprite 指针 | `cursor-theme.test.ts`、`PreferencesDialog.test.tsx` |
| 图层多选 | 混合选择普通图层、空组和嵌套组后拖拽或按住 `Alt` 拖拽 | 所有顶层选中对象统一移动或复制，组覆盖的后代不重复处理；复制、移动分别只产生一个撤销步骤，且不能拖入自身或后代 | `workspace.test.ts`、`layer-panel-layout.test.ts`、待补桌面拖拽场景 |
| 图层多选 | 右键混合选中的任一图层或组并修改属性 | 不清除当前混合选择；名称、混合模式、不透明度、显示颜色和描述均可覆盖整批对象且实时预览，未修改字段保持原值，关闭后只产生一次撤销 | `LayersPanel.test.tsx` |
| 图层拖放 | 在所属组内原地拖动，或拖到另一个图层组内容区域 | 原地无有效落点时保持原层级和顺序；容器落点显示整行蓝框且不显示插入位置指示线，松手后对象进入目标组 | `layer-panel-layout.test.ts`、待补桌面拖拽场景 |
| 直线方向 | 已有画笔或橡皮擦直线锚点时按住 `Shift+Ctrl` 绘制 | 阶梯数值 `1` 吸附到 8 个方向；数值大于 `1` 吸附到 12 个方向；预览和提交终点一致且一次撤销 | `pixel-line.test.ts`、`shortcuts.test.ts`、待补画布输入场景 |
| 透明背景 | 在首选项输入非预设棋盘格尺寸并应用 | 接受 `1-256 px` 整数，重开首选项和切换工程后保持；缺失或无效存储值回退到 `16 px` | `file-preferences.test.ts`、待补 `PreferencesDialog` 组件场景 |
| 图层批量属性 | 混合多选图层与图层组后打开属性弹窗 | 名称、混合模式、不透明度、显示颜色和描述都可覆盖整批对象；输入期间实时预览，关闭后整批变化只产生一次撤销 | `LayersPanel.test.tsx` |
| 图层多选拖放 | 混合选择图层与图层组后拖到栏目顶部、底部或其他容器 | 所有选中对象严格保持拖动前从上到下的相对顺序；展开组及其内容的选中状态在移动后完整保留 | `LayersPanel.test.tsx`、`layer-operations.test.ts` |
| 图层拖放反馈 | 把图层或混合选择拖到展开的图层组 | 蓝色容器框只覆盖图层组标题行，不插入额外网格项、不扩大行高，也不包住组内图层 | `LayersPanel.test.tsx` |
| 新建图层 | 直接选中图层组后新建图层 | 新图层位于所选组外并与该组同级；只有选中组内图层时，新图层才进入该组 | `workspace.test.ts` |
| 开发热更新 | 图层拖拽预览结构更新时仍保留旧版悬浮状态 | 新组件兼容旧状态并自动恢复，不因缺失新字段导致图层栏目崩溃或编辑器黑屏 | 待补 HMR 集成场景 |
| 直线方向 | 阶梯数值设为 `3` 后按住 `Shift+Ctrl` 从任意方向连接两点 | 约束后的每个主轴阶梯严格包含 3 个像素，起始阶和结束阶均不缩短；反向连接得到相同像素集合 | `pixel-line.test.ts` |
| 图层拖放预览 | 混合选择展开组、组内图层和根图层后开始拖拽 | 悬浮预览按当前可见顺序列出全部选中行；真正移动时仍只处理未被父组覆盖的顶层对象，超过四行以数量汇总 | `LayersPanel.test.tsx` |
| 图层复制拖放 | 多选图层或组后按住 `Alt` 拖到原选择上方或下方 | 源行可作为复制插入锚点并显示对应蓝线；复制对象落在所选一侧，原对象保持不变 | `layer-panel-layout.test.ts`、`LayersPanel.test.tsx` |
| 图层新建与粘贴 | 有选中对象时新建或粘贴图层、图层组和组合，无选择时执行相同操作 | 有选择时整批新对象位于选中对象同级的正上方；无选择时位于根层最上方；内部组结构和一次撤销保持不变 | `workspace.test.ts`、`layer-operations.test.ts` |
| 图层批量属性性能 | 在较大画布中连续拖动批量不透明度或快速修改属性 | 表单反馈立即更新，文档预览按约 40 ms 合并为最新一次变化；停止操作、关闭或提交时最终值完整应用且仍只产生一次撤销 | `LayersPanel.test.tsx`、定向画布性能检查 |
| 数值输入 | 在统一数值输入框键入或粘贴字母、中文及无效符号 | 非数值字符不会进入输入值；数字、小数、括号和 `+ - * /` 数值表达式仍可正常计算 | `NumberInput.test.tsx`、`numeric-expression.test.ts` |
| 图层复制拖放 | 按住 `Alt` 将全部已选图层或组拖到最后一个可见行下方的空白区域 | 即使拖动源覆盖全部可见行，也能命中根层最底部并完成复制；蓝线紧贴最后一个可见图层或组下方，不落在栏目容器底边 | `LayersPanel.test.tsx` |
| 图层拖放预览 | 多选对象中包含折叠组内的不可见行后开始拖拽 | 悬浮预览显示所有可见选中行，并以 `+N` 准确汇总未逐行显示的选中对象 | `LayersPanel.test.tsx` |
| 直线方向 | 阶梯数值设为 `6`，从已经绘制的锚点按住 `Shift+Ctrl` 连接斜线 | 起始锚点计入第一阶，第一阶与后续阶梯采用相同的 6 格计数，预览和最终像素一致 | `pixel-line.test.ts`、`tools.test.ts` |
| 快捷键弹窗 | 向下拉大快捷键设置弹窗 | 左侧分类和右侧快捷键内容随可用高度同步伸展，底部操作栏保持贴底，不出现无用途的空白区域 | `ShortcutDialog.test.tsx`、待补桌面视觉场景 |
| 弹窗缩放 | 指针位于弹窗右下角附近但未接触可见边框 | 只有以边框为中心的紧凑命中带显示缩放指针，内容区附近不得误触缩放 | `ModalShell.test.tsx`、待补桌面指针场景 |
| 弹窗拖动 | 将任意弹窗拖到软件窗口边缘或暂时移出窗口 | 拖动期间保持指针捕获和连续位置更新，不闪烁、不跳回，松开后保留可恢复操作的标题区域 | `ModalShell.test.tsx`、待补桌面窗口边缘场景 |

| 图层底部落点 | 把图层或组拖到最后一个可见对象下方 | 蓝色插入线紧贴最后一行下边缘，并与普通插入线一样同时显示左右三角端点 | `LayersPanel.test.tsx` |
| 图层组粘贴选择 | 复制包含嵌套组与图层的组合后粘贴 | 新建组、嵌套组及其全部后代图层同时保持选中；撤销恢复旧选择，重做恢复完整新选择 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 图层组粘贴折叠 | 分别复制折叠和展开的图层组后粘贴 | 新组保留源组的折叠状态；撤销和重做不改变该状态 | `workspace.test.ts` |
| 图层批量颜色 | 多选图层或组后在属性弹窗单击显示颜色 | 第一次单击即覆盖整批目标并实时预览，不需要重复点击 | `LayersPanel.test.tsx` |
| 图层组显示颜色 | 为图层组设置显示颜色 | 组内所有后代图层在眼睛图标左侧显示继承色条；后代自身颜色不覆盖组继承颜色 | `LayersPanel.test.tsx` |
| 快捷键导出 | 点击快捷键设置中的“导出”并选择文件路径 | Windows 保存对话框正常出现，确认后写入合法 UTF-8 JSON；取消不写文件 | `ShortcutDialog.test.tsx`、Rust 对话框测试 |
| 弹窗边界拖动 | 从标题区域拖动弹窗到软件窗口边缘，并从标题下方空白区域继续拖动 | 拖动连续且不闪烁；允许窗口主体向下移出视口，但始终保留可找回的标题区域 | `ModalShell.test.tsx`、桌面人工场景 |
| 选区倾斜 | 从选区四边中点外侧的倾斜命中区拖动 | 上下命中横向倾斜，只改变横向像素偏移；左右命中纵向倾斜，只改变纵向像素偏移；使用独立指针、实时预览并支持一次撤销 | `canvas-input.test.ts`、`tools.test.ts`、`canvas-visuals.test.ts` |
| 快捷键导入 | 选择合法的快捷键 JSON 配置 | 配置立即载入草稿，并在快捷键弹窗内明确提示导入成功；后续编辑会清除旧提示 | `ShortcutDialog.test.tsx` |
| 图层组显示颜色 | 最外层组与嵌套组均设置显示颜色 | 最外层组的颜色覆盖其全部后代组和图层；只有外层祖先均未设置颜色时才使用更内层对象的颜色 | `LayersPanel.test.tsx` |
| 图层批量属性 | 同时明确选中父组、子组及组内图层后打开属性弹窗 | 属性目标包含全部明确选中的对象，不使用拖拽结构去重规则；统一编辑实时作用于父组、子组和图层 | `LayersPanel.test.tsx` |
| 选区倾斜指针 | 指针进入横向或纵向倾斜命中区 | 分别显示用户提供的新横向和纵向像素指针，热点保持在图标中心 | 资源尺寸检查、待补桌面视觉场景 |
| 对称轴 | 分别或同时开启上下、左右、右上左下、左上右下对称后使用铅笔、橡皮擦或形状工具 | 所有轴使用同一可移动轴心；组合变换计算闭包并去重，中心与轴线像素只写入一次；预览与提交一致 | `symmetry.test.ts`、`tools.test.ts`、待补桌面场景 |
| 对称选区 | 开启任意对称轴后使用矩形、椭圆、自由套索、多边形套索或魔棒 | 每个原始选区生成对应镜像并合并，矩形画布中斜向镜像的越界像素被丢弃 | `symmetry.test.ts`、`selection.test.ts` |
| 对称填充 | 开启多轴后使用连续或不连续油漆桶 | 对每个去重后的镜像种子分别执行相同填充规则，合并为一次像素编辑和一次撤销 | `tools.test.ts` |
| 图层组拖放选择 | 只选中一个组并拖动到任意层级 | 移动后仍只显式选中该组，不把组内后代图层或子组改为显式选中 | `LayersPanel.test.tsx` |
| 嵌套组拖放命中 | 在任意深度的组、子组和子层之间拖动组 | 只有指针进入目标组标题行中部才显示蓝色容器框；子层行不提前触发父组蓝框；标题上下区域提供同级插入线并支持任意嵌套排序 | `layer-panel-layout.test.ts`、`LayersPanel.test.tsx` |
| 未来计划 | 打开“帮助 > 未来计划”并切换中英文 | 列表完整显示既定功能及完成状态；对称轴与动画显示已完成，其余计划显示未完成 | 菜单组件测试、桌面视觉检查 |
| 更新日志 | 打开“帮助 > 更新日志” | 只显示最近一次正式打包版本的归档内容和版本号，不混入根日志中的未发布改动 | `LatestReleaseDialog.test.tsx`、菜单组件测试 |
| 对称轴控制 | 在支持工具的属性栏切换四个对称图标并重置轴心 | 上下、左右、右上左下、左上右下可任意组合；选中状态使用品牌蓝；重置恢复当前画布中心 | `symmetry.test.ts`、工具属性栏组件测试 |
| 对称轴位置 | 拖动画布中的对称轴或中心点 | 蓝色辅助线为实线；单轴拖动沿法线调整共享轴心，中心点可二维移动，位置吸附到半像素且不进入撤销历史 | `symmetry.test.ts`、待补桌面场景 |
| 对称笔刷预览 | 开启任意对称轴后悬停或绘制 | 只在鼠标所在的原始位置显示笔刷预览，不在镜像侧重复预览；实际像素仍写入全部镜像位置 | `tools.test.ts`、待补画布渲染测试 |
| 对称选区变换 | 开启任意对称轴后移动、缩放、旋转或倾斜选区内容 | 原始操作与全部镜像结果使用同一轴心、同一预览和一次撤销；源选区与镜像选区不会因重复闭包生成额外副本 | `symmetry.test.ts`、`tools.test.ts`、待补桌面场景 |

## 新增规则

| 动画帧与单元格 | 帧/单元格多选、属性、复制粘贴、移动与删除 | 当前帧内容、空帧状态、单元格不透明度和选择范围保持一致，并可撤销恢复 | `animation.test.ts`、`LayersPanel.test.tsx`、`workspace.test.ts` |
| 画布选区快捷键 | 已有矩形或椭圆选区时按 `Ctrl+D` | 画布选区立即取消；该快捷键仅在没有画布选区时继续用于复制单元格 | `App.tsx`、快捷键桌面场景 |
| 动画帧落点 | 拖动帧到其他帧前后位置 | 蓝色落点线从帧头贯穿到动画网格底部，明确覆盖全部图层行 | `LayersPanel.test.tsx`、待补桌面场景 |
| 动画多选外框 | 使用 `Ctrl` 离散选择多个帧或多个单元格 | 每类对象只显示一个包围全部选择范围的蓝色外框；选中帧列背景统一为 `#20242d` | `LayersPanel.test.tsx`、待补桌面视觉场景 |
| 动画选中层级 | 图层栏顶部、左右停靠及滚动后选择单元格 | 单元格外框不被标题、停靠或滚动层遮挡，边框完整可见 | `LayersPanel.test.tsx`、待补桌面视觉场景 |
| 框选比例修饰键 | 矩形/椭圆框选时按 `Shift` 加选，或按 `Ctrl` 创建形状 | `Shift` 只负责加选，不限制比例；`Ctrl` 才限制为 1:1；`Shift+Ctrl` 同时加选并限制比例 | `canvas-input.test.ts`、`CanvasStage.tsx` |
| 图层动画帧复制 | 复制一个已有动画图层并粘贴到工程 | 新图层包含源图层的全部帧、空帧状态、每帧时长和 cel 内容，不只复制当前帧 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 动画整列选框 | 选择帧并向下滚动图层栏 | 蓝色外框包含顶部帧数栏和全部单元格；sticky 帧头在选框之上显示，滚动时不互相覆盖 | `LayersPanel.test.tsx`、待补桌面视觉场景 |
| 动画单元格层级 | 选择单元格并上下滚动动画网格 | 单元格外框不得盖住帧数栏或帧头，顶部帧数栏始终处于最高层 | `LayersPanel.test.tsx`、待补桌面视觉场景 |
| 动画单元格批量粘贴 | 多选单元格后复制，再选择目标单元格粘贴 | 所有源单元格按相对行列位置写入目标，允许覆盖空单元格并完整建立一次撤销记录 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 画布尺寸预览撤销边界 | 打开调整画布尺寸并拖动四条边，随后执行 Undo | 拖动预览只改变临时画布范围，不写入文档撤销；只有点击完成后的尺寸变更进入一次撤销 | `workspace.test.ts`、`CanvasResizeDialog.test.tsx` |
| 调整结果撤销 | 在调整弹窗修改数值并应用，随后 Undo/Redo | 画布像素、调整数值对应的效果和选区范围一起恢复，不残留预览效果 | `workspace.test.ts`、`AdjustmentDialog.test.tsx` |
| 调整期间选区变换 | 调整弹窗打开后移动、变换或减选已有选区 | 调整数值不归零；新选区成为调整目标；选区外和移动前的背景不被调整污染 | `AdjustmentDialog.test.tsx`、待补桌面场景 |
| 动画帧非连续选框 | 使用 `Ctrl` 选择不相邻帧并滚动帧栏 | 每个连续帧区间各自显示蓝色外框，正文外框从帧数栏下方开始，不覆盖 sticky 帧头 | `LayersPanel.test.tsx` |
| 动画帧栏拖动指针 | 拖动图层名称区与帧区之间的分隔线并移出分隔线 | 整个拖动手势始终显示横向缩放指针，结束或取消后恢复正常指针 | `LayersPanel.tsx`、待补桌面场景 |
| 动画播放按钮 | 播放动画并观察播放按钮 | 播放状态保留背景反馈，但不显示蓝色边框；图标在按钮中心 | `LayersPanel.tsx`、待补桌面场景 |
| 调整操作撤销 | 确认画布大小、图像大小、颜色调整后执行 Undo/Redo | 每类调整确认只写入一个历史条目，Undo/Redo 恢复像素、尺寸和选区 | `workspace.test.ts`、`AdjustmentDialog.test.tsx` |
| 调整弹窗中的选区变换 | 打开编辑调整弹窗后移动或变换已有选区 | 选区变换不会被旧调整快照覆盖；后续调整从变换后的状态继续 | `AdjustmentDialog.test.tsx`、待补桌面场景 |
| 轴向移动画布范围 | 调整画布尺寸预览时按住 `Shift` 拖动画布范围 | 位移锁定水平或垂直主轴，预览与确认结果一致 | `CanvasStage.tsx`、待补桌面场景 |
| 框选加选指针 | 已有选区内按住 `Shift` 加选并移动指针 | 始终显示框选创建指针，不闪烁禁止指针 | `CanvasStage.tsx`、待补桌面场景 |
| 弹窗输入初始焦点 | 打开图层属性、帧属性、画布大小或图像大小 | 第一个相关输入框自动获得焦点并全选现有内容 | `LayersPanel.tsx`、`CanvasResizeDialog.test.tsx`、`ImageResizeDialog.test.tsx` |
| 动画帧整列外框 | 单选或连续选择帧并观察帧头与 cel 列交界 | 帧头和正文共用一条连续外框，交界处没有横向蓝线，不显示成上下两个框 | `LayersPanel.test.tsx` |
| 新工程初始 cel | 新建任意颜色模式工程并在图层栏查看第一帧 | 文档创建时首层首帧已经存在独立 cel，不依赖图层栏首次渲染补建 | `animation.test.ts`、`workspace.test.ts` |
| 画布尺寸预览撤销 | 打开画布大小并连续键入宽高、四边数值或拖动蓝色边界后按 `Ctrl+Z`/`Ctrl+Y` | 弹窗内部按完整数值逐步撤销和重做，不按单个字符回退；预览历史不污染文档历史，确认后整体只产生一个文档撤销步骤 | `CanvasResizeDialog.test.tsx`、`workspace.test.ts` |
| 调整期间加减选 | 调整弹窗已有非零参数时按 `Shift` 或右键建立加选、减选 | 按下和拖拽期间保持操作前的调整画面，松开提交选区后才按新范围重算，不闪回未调整状态 | `AdjustmentDialog.test.tsx`、`adjustment-preview-lifecycle.test.ts` |
| 调整期间内容变换 | 调整弹窗已有非零参数时移动、缩放、旋转或倾斜选区内容 | 手势全程显示相同调整效果；内部仍基于未调整像素计算，松开后参数和结果不消失、不重复叠加 | `AdjustmentDialog.test.tsx`、`adjustment-preview-lifecycle.test.ts` |
| 调整与画布操作撤销顺序 | 确认一次颜色调整后再执行一次像素或选区操作，并连续 Undo/Redo | 第一次只撤销最后的画布操作，第二次才撤销调整；Redo 按原顺序逐项恢复，两个动作不得合并 | `workspace.test.ts`、`history.test.ts` |
| 动画单元格粘贴锚点 | 从左到右多选多个 cel 后复制，再选择目标第一格粘贴 | 复制块的第一格对齐当前目标格，其余 cel 按相对行列展开，不要求选择源块最后一格对应位置 | `workspace.test.ts` |
| 新工程空首 cel | 新建空白工程并查看图层栏第一帧 | 首层首帧即使没有像素也显示空 cel 槽位，可点击、选择和右键粘贴；不显示实心内容标记 | `animation.test.ts`、`LayersPanel.test.tsx` |
| 动画拖拽门槛 | 在蓝色选框外长按未选中帧或单元格，再从蓝色选中框拖动 | 长按把对象追加到既有多选且不移动；只有已选中帧/单元格的蓝色外框区域才开始拖拽移动 | `LayersPanel.test.tsx` |
| 色盘方形恢复 | 浮动色盘经过停靠变化后点击恢复方形 | 字段区域重新测量，色盘恢复方形并贴合当前栏目或停靠区域，不需要再次拖动分隔线 | `ColorPanel.test.tsx`、待补桌面视觉场景 |
| 调色板锁定编辑 | 锁定调色板后尝试修改颜色，再解锁并从现有颜色编辑控件调整已选颜色 | 锁定时不改变色盘网格；解锁后不插入内嵌编辑器，现有前景/背景色编辑实时写回共享调色板，相关色块同步且可撤销 | `workspace.test.ts`、`PalettePanel.test.tsx` |
| 调整画布后的工具预览 | 放大画布并确认调整后立即移动铅笔或橡皮擦 | 旧画布尺寸预览、连续绘制锚点和工具预览全部清理，不出现偏移或残影 | `CanvasResizeDialog.test.tsx`、待补桌面场景 |
| 动画长按范围选择 | 长按第一个帧或空单元格并移动到后续对象 | 按移动路径选择连续帧/单元格，空单元格同样可选；未命中蓝色外框时不触发移动 | `LayersPanel.test.tsx`、待补桌面场景 |
| 动画范围选择实时预览 | 长按帧或单元格并从第一个对象拖到后续对象 | 蓝色范围框随着指针经过的对象即时变化；帧操作只渲染帧临时选区，单元格操作只渲染 cel 临时选区，不互相覆盖 | `LayersPanel.test.tsx`、待补桌面场景 |
| 动画选区互斥状态 | 已有帧选区时再选择单元格，或已有单元格选区时再选择帧 | 新选择类型立即清空另一类显式选区和临时描边，画面中任何时刻只保留帧或 cel 一类蓝框 | `workspace.test.ts`、`LayersPanel.test.tsx` |
| 已选帧跨行范围拖选 | 选中第一帧后从蓝框或帧头长按，指针向下进入 cel 行并移动到第四帧列 | 临时整列蓝框实时扩展到 1-4 帧，松开后只保存帧选区，不误选 cel | `LayersPanel.test.tsx` |
| 选区方向键微调 | 创建包含像素的选区后依次按上下左右方向键 | 每次把选区内容与边界移动 1 像素，每次按键可独立撤销，输入框获得焦点时不触发 | `workspace.test.ts`、待补画布键盘组件测试 |
| 框选比例修饰键 | 无选区时按 Shift 框选；已有选区时分别用 Shift 加选和 Shift+Ctrl 加选 | 新建时 Shift 为 1:1；Shift 加选为自由比例；Shift+Ctrl 加选和其他 Ctrl 框选为 1:1 | `canvas-input.test.ts` |
| 多图层洋葱皮 | 多个可见图层在相邻帧分别有内容，并存在不透明当前帧内容 | 相邻帧按完整可见图层合成后显示在当前帧上方，不会被当前帧的其他不透明图层整体遮住 | `onion-skin.test.ts`、待补画布渲染顺序测试 |
| 画布调整后的笔刷预览重建 | 放大画布后不落笔，直接在新画布范围移动铅笔 | 指针文档坐标和完整笔刷预览立即按新尺寸重算，不需要先点击绘制恢复 | `document.test.ts`，待补 `CanvasStage` 组件测试、桌面场景 |
| 当前颜色入调色板 | 前景色或背景色不在当前调色板时查看颜色编辑区域 | 显示加入调色板按钮；颜色已经存在时隐藏按钮并让颜色值行占满剩余宽度 | `ColorPicker.test.tsx`、`workspace.test.ts` |
| 调整预览点击稳定性 | 调整弹窗已有非零参数时点击或按下选区内容 | 指针按下到松开期间保持同一调整结果，不短暂闪回原色或重复叠加调整 | `AdjustmentDialog.test.tsx`、`adjustment-preview-lifecycle.test.ts` |
| 空 cel 视觉语义 | 新工程或空白动画槽位没有像素内容 | 第一帧槽位可选择和粘贴，但不绘制空心方框；其他无内容槽位保持空白 | `LayersPanel.test.tsx`、`animation.test.ts` |
| 多帧默认导出 | 帧数大于 1 时打开导出设置 | 默认格式自动选择 GIF；单帧工程继续使用用户配置的静态图片格式 | `App.test.tsx`、`gif.test.ts` |
| GIF 动画方向 | 分别导出正向、反向、正向往复和反向往复 | 四帧输入依次生成 `1234`、`4321`、`123432`、`432123`；方向选项显示简短描述且不再提供旧“往复”选项 | `gif.test.ts`、导出弹窗组件测试 |
| 颜色模式排序 | 在首选项颜色页拖拽颜色模式 | 不显示复选框或上下移动按钮；拖拽后顺序立即更新，至少保留一个可用模式 | `PreferencesDialog.test.tsx` |
| 当前颜色入调色板反馈 | 点击前景色或背景色的“加入调色板”按钮 | 当前颜色立即加入共享调色板，按钮同一帧消失，颜色编辑区域不需要关闭重开 | `ColorPicker.test.tsx`、`workspace.test.ts` |
| 图层设置实时控制 | 调整缩略图/帧信息滑块、百分比输入滑块、洋葱皮参数或点击重置 | 图层栏与画布实时更新；重置恢复默认值；关闭后保持已应用设置，弹窗使用紧凑宽度 | `LayersPanel.test.tsx`、`file-preferences.test.ts` |
| 洋葱皮快捷开关 | 点击“第 N/N 帧”右侧的洋葱皮按钮 | 立即切换洋葱皮显示并保存设置，不写入文档撤销历史 | `LayersPanel.test.tsx`、`onion-skin.test.ts` |
| 导出弹窗动态尺寸 | 静态图片格式与 GIF 格式之间切换 | 弹窗在最小/最大限制内按内容高度自适应，GIF 范围、方向和缩放控件始终完整可见且无内部滚动条 | 导出弹窗组件测试、1024 × 640 视觉检查 |

- 每个用户报告的回归问题至少增加一行。
- “待补”场景被修改时必须转为自动化测试，不能只改为“手动通过”。
- 像素结果使用数组或图像像素断言；交互结果同时断言状态与可见反馈。
- 性能回归写入 `performance-baseline.md`，不混用功能正确性断言。
| Animation frame keyboard navigation | With an animation timeline active, press Left/Right outside text inputs and dialogs | Left/Right changes the active frame by one step without changing canvas selection geometry; inputs, dialogs, and selection transform keep their existing keyboard behavior | `workspace.test.ts`, `LayersPanel.test.tsx` |
| Mirrored floating selection cleanup | Select, paste, toggle outline, mirror the floating selection, move it, then undo/cancel | The old pixels are restored exactly once and no duplicate floating pixels remain | `workspace.test.ts`, `CanvasStage.tsx` |
| Palette foreground/background synchronization | Unlock palette editing and adjust the active foreground or background color in the color editor | Only the palette entry associated with that color changes in real time; locking only disables palette synchronization and does not alter visible selection state | `workspace.test.ts`, `PalettePanel.tsx` |
| Linked animation cels | Select cels and choose Connect from the cel context menu | Cels are linked per layer, the foremost non-empty cel is the source, edits are shared, and one undo restores all links and surfaces | `animation.test.ts`, `workspace.test.ts` |
| Initial layer settings layout | Open layer settings and compare the restored early layout | The dialog uses the early balanced layout, keeps controls visible without accidental scrolling, and preserves reset/save/cancel actions | `LayersPanel.test.tsx` |
| 最近文件失效路径 | 重命名或删除最近列表中的文件后返回首页 | 检测到原路径不存在时提示用户并自动移除该条最近记录；现存但无法解析的文件仍保留 | `HomeWorkspace.test.tsx`、`platform_files.rs` |
| 图层设置窄版布局 | 打开图层设置并观察默认宽度、洋葱皮双列控件 | 弹窗不被首选项最小宽度规则误判，默认使用窄版宽度；帧数、不透明度和颜色输入在两列内对齐 | `ModalShell.test.tsx`、`LayersPanel.test.tsx` |
| 连接单元格视觉 | 在紧凑、标准和缩略图密度下连接相邻 cel，并在连接段中夹入独立 cel | 连接段与独立 cel 保持同高并位于网格分隔线之上；缩略图自身尺寸不因左右连接而放大 | `LayersPanel.test.tsx`、桌面视觉场景 |
| 连接单元格粘贴 | 复制有内容 cel 后粘贴到已连接 cel 中的任意一格 | 粘贴内容写入共享 cel 源，全部连接格同步变化且连接关系保留；一次 Undo/Redo 恢复整个连接组 | `workspace.test.ts` |
| 镜像浮动选区再次移动 | 移动选区后连续执行 `Shift+H`/`Shift+V`，每次镜像后继续拖动 | 镜像同步更新像素、掩码和移动缓存；任意次数镜像后移动都保持当前方向，不恢复旧内容 | `tools.test.ts`、`workspace.test.ts` |
